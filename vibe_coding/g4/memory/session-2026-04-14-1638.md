# Session 2026-04-14-1638 — g4

## What was done
- Created new project `g4` at `/home/ori/fun/vibe_coding/g4/`
- Installed Ollama on Ubuntu 22
- Pulled `gemma4:e2b` model (7.2GB, Q4_K_M) — the only model that fits on GTX 1060 6GB
- Built terminal chatbot (`chat.py`) — fixed Hebrew RTL via python-bidi
- Built web chatbot (`app.py` + `templates/index.html`) — Flask + SSE streaming
- Added Markdown (marked.js) + LaTeX (KaTeX) rendering to web UI
- Fixed `defer` loading issue for KaTeX scripts

## Hardware
- GPU: GTX 1060 6GB — model weights fit mostly on GPU, some RAM needed
- RAM: 16GB — need ~5.4GB free; close Zoom/browser tabs before running
- Key finding: `gemma4:e2b` is 7.2GB on disk (MoE 5.1B total, 2.3B active)

## Known issues / next steps
- Markdown+LaTeX rendering was fixed (defer removed) but not confirmed working — verify on next session
- `chat.py` kept as CLI fallback (with python-bidi fix)
- No memory directory in git yet (`.gitignore` may need update)

## Files
- `app.py` — Flask server with SSE streaming to Ollama
- `chat.py` — Terminal chatbot (fallback)
- `templates/index.html` — Web UI with RTL, Markdown, LaTeX
