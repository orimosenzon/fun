# סשן 2026-05-25-1324 — chidonait (מצגת משקיעים לחידונאית)

## מה נעשה בסשן
- נחקר הפורמט "האחוזון העליון" (גרסת *The 1% Club*) כבסיס לז'אנר החידות.
- נבנתה **מצגת משקיעים אינטראקטיבית** ב-HTML: `index.html` — 16 שקופיות, RTL, מנוע סליידים מותאם.
- נוסף תיעוד: `memory/project-knowledge.md`.
- יוצא **PPTX סופי**: `chidonait-pitch.pptx` — 19 שקופיות, 16:9, כל שקופית כתמונה ב-2560×1440.

## החלטות מפתח
- **מודל עסקי:** מותג אישי + 6 ערוצי הכנסה (תוכן ממומן, רישוי לטלוויזיה, ספרים/משחקים, חידות ממותגות, סדנאות, אפליקציה במנוי).
- **קונספט מנחה:** המצגת עצמה היא הדגמת המוצר — 4 חידות אינטראקטיביות שהמשקיע פותר תוך כדי (רצף מצולעים, חשבון אמוג'י, ספירת ריבועים, הטעיה מילולית).
- **פורמט:** HTML אינטראקטיבי כמקור האמת; PPTX נגזר ממנו ע"י רינדור headless.
- ב-PPTX: לחידות יש שקופית "שאלה" ואחריה שקופית "תשובה" — חשיפה לינארית בהצגה.

## טכני (לזכור לייצוא חוזר)
- ייצוא PPTX: רינדור עם `google-chrome-stable --headless --force-device-scale-factor=2 --window-size=1280,720` לכל שקופית (#hash), הזרקת CSS להסתרת chrome + ביטול אנימציות, ובניית הקובץ ב-`python-pptx`.
- ניווט ב-HTML דרך hash (#n) — מאפשר רינדור שקופית בודדת.

## פתוח לפעם הבאה (פלייסהולדרים להחלפה)
- שם המותג (כרגע "חידתי"), `[השם שלך]`, פרטי קשר (`[מייל]`/`[טלפון]`/`[@החשבון]`).
- כל מספרי הטראקשן/גיוס מסומנים "להמחשה · לעדכון".
- אופציונלי: דחיסת PPTX ל-JPEG (~4MB), נספח פיננסי, case study של חידה ויראלית, לוגואים.

## הערה
- לבקשת המשתמש — הפרויקט **לא** נוסף ל-`index.html` (דף הפרויקטים).
