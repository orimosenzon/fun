# Session — 11 March 2026

## What we did

### פיצ'ר חדש: Vector Steps
- הוספנו tab חדש "Vector Steps" שמדגים אנלוגיה וקטורית בשני שלבים
- שלב 1: משתמש מכניס מילה A ומילה B → רואה שתי נקודות על מישור 2D וחץ ביניהן (= B − A)
- שלב 2: משתמש מכניס מילה C → רואה גם C, גם D = C+(B−A), וחץ מ-C ל-D
- תוצאה מוצגת בפורמט: `A : B :: C : D`
- Backend: `explore_analogy()` ב-`analogy_engine.py` + route `/api/explore` ב-`app.py`

### תיקוני באגים
- פורט כפול בהדפסה: Flask debug mode מריץ שני תהליכים — תוקן עם `WERKZEUG_RUN_MAIN` + `FLASK_PORT` env var
- כפתור נתקע על "Loading...": טיפול ב-503 שמחזיר HTML במקום JSON — נוסף try/catch + בדיקת `res.ok`

### ניקוי — הסרת פיצ'רים
הוסרו לפי בקשת המשתמש:
- Quiz Mode
- Build Your Own
- Vector Map

נשארו רק: Nearest Words + Vector Steps

## Next steps
- שיפורים ל-Vector Steps (TBD)
- שמות לטאב החדש — המשתמש לא החליט עדיין
