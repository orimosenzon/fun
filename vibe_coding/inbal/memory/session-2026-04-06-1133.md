# סשן 2026-04-06 — חידוש WhatsApp Token + בדיקת התראות אדמין

## מה נעשה
- חודש WhatsApp access token דרך Graph API Explorer (הדרך הישנה דרך developers → API Setup הוסרה מהממשק)
- Token עודכן ב-.env.local וב-Vercel → redeploy
- הסימולציה (`simulate-standby.ts`) הורחבה — עכשיו בודקת 3 הודעות:
  1. `standby_notification` → לאורי (תלמיד)
  2. `admin_spot_opened` → לענבל (מקום נפתח)
  3. `admin_spot_taken` → לענבל (מקום נתפס)
- נוצרה תבנית חדשה `admin_spot_taken` ב-Meta (Hebrew, Utility) — כרגע בבדיקה

## סטטוס תבניות
- `standby_notification` — פעילה ✓
- `admin_spot_opened` — פעילה ✓
- `admin_spot_taken` — בבדיקה ⏳ (נוצרה היום)

## Next Steps
1. המתן לאישור `admin_spot_taken` (כמה שעות)
2. הרץ סימולציה מלאה לבדיקת כל 3 ההודעות:
   ```
   PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH" node node_modules/.bin/tsx scripts/simulate-standby.ts
   ```
3. ודא שענבל קיבלה 2 הודעות WhatsApp (admin_spot_opened + admin_spot_taken)
4. ניקוי: `...tsx scripts/cleanup-simulation.ts`

## הערות
- Meta שינו את ממשק ה-developers — API Setup כבר לא נמצא שם. ליצירת token: developers.facebook.com/tools/explorer
- Token פג כל ~60 יום — יש לחדש מ-Graph API Explorer
