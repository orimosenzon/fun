# סשן 2026-04-06-0900 — ענבל קרמיקה

## מה קרה

### הוספת התראות WhatsApp לענבל
- נוסף `sendAdminSpotOpened` — נשלח לענבל כשמקום מתפנה (כולל רשימת המתנה + מי קיבל הזמנה)
- נוסף `sendAdminSpotTaken` — נשלח לענבל כשמישהי תופסת מקום מרשימת המתנה
- שופץ `lib/whatsapp.ts` — helper פנימי `sendTemplate` מאחד את לוגיקת השליחה
- עודכן `lib/standby.ts` — מוסיף התראה לאדמין אחרי שליחת ההזמנה לתלמידה
- עודכן `app/api/sessions/[id]/register/route.ts` — מוסיף התראה לאדמין כשמקום נתפס מסטנדביי

### Templates ב-Meta
- `admin_spot_opened` — אושר ✅ (בסטטוס "פעיל")
- `admin_spot_taken` — אושר ✅ (בסטטוס "פעיל")

### Vercel
- קוד נפרס בהצלחה (commit `a01a636`)
- Vercel token חדש נשמר ב-`~/.vercel-token` (inbal-deploy-2)

## מה לא הושלם
- WhatsApp access token פג ב-31 במרץ — הסימולציה נכשלה בגלל זה
- **לא נבדקה הבדיקה end-to-end** (צריך token חדש קודם)

## Next Steps
1. **חדש WhatsApp token** — developers.facebook.com → inbal-ceramics → WhatsApp → API Setup → Generate access token
2. הרץ: `./scripts/renew-whatsapp-token.sh NEW_TOKEN`
3. עדכן token ב-Vercel: Settings → Environment Variables → WHATSAPP_ACCESS_TOKEN → Redeploy
4. הרץ סימולציה: `PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH" node node_modules/.bin/tsx scripts/simulate-standby.ts`
5. ודא שאורי וענבל קיבלו WhatsApp
6. נקה אחרי: `PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH" node node_modules/.bin/tsx scripts/cleanup-simulation.ts`
