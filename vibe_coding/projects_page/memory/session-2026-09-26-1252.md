# סשן 2026-09-26 (2)

## מה נעשה
- נוספה מתחת לתוכן העניינים גלריה בשם **"פרויקטים live: נסו עכשיו בדפדפן"**: 22 כרטיסים, בכל אחד צילום מסך של האפליקציה רצה, וכל כרטיס מקשר לאפליקציה החיה.
- ההחלטה מי נכנס נעשית ב-`try_projects` ב-`build.py`: כל כרטיס עם כפתור `live` או `primary`, חוץ ממה שמסומן `"try": false` ב-`projects.json`.
- `shots.py` חדש: מצלם כל אפליקציה ב-Chromium בלי חלון, 640×400 WebP, לתוך `shots/<anchor>.webp`. בלי ארגומנטים הוא מצלם רק מה שחסר. `TWEAKS` מגדיר לכל אפליקציה מה ללחוץ, להקליד או לטעון לפני הצילום (2048 באמצע משחק, Letras עם תוצאות חיפוש, Fable ו-Opus5 אחרי ההתנעה, Animate עם a1.clp).
- הנתון "Live Demos" בכותרת סופר עכשיו את אותה רשימה (22 במקום 5).
- בנייד הגלריה מציגה שני כרטיסים בשורה.
- ב-bf7c097 (קומיט של סשן השקיפות) נכנס בטעות index.html שנבנה מהקבצים שלי לפני שהם קומשו, כי ה-hook בונה מחדש בכל קומיט. לכן זמן קצר האתר הציג גלריה בלי תמונות. הקומיט הזה משלים את החסר.

## מצב נוכחי
- הוצאו מהגלריה (`"try": false`):
  - Smart Business Search ו-Historical Empires, **שבורים באתר החי**: `DeletedApiProjectMapError`, כלומר פרויקט הענן של מפתח Google Maps נמחק.
  - Remez, כי ה-Space פרטי (401).
  - Haskala, כי הקישור הראשי הוא דף נחיתה.
- מודל מנהטן עובד, אבל מפת הבסיס של Carto דורשת עכשיו מפתח ומציגה סימן מים "API KEY REQUIRED".

## צעדים הבאים
1. מפתח Google Maps חדש ל-ori/index.html ול-empires.html, ואז להסיר את `try: false` ולהריץ `shots.py ori empires`.
2. להחליף את ספק האריחים של מודל מנהטן (למשל אריחים כהים של OSM או Stadia) ולצלם מחדש.
3. להמשיך מהסשן הקודם: photomap ו-transparency בלי כרטיס, תיבת חיפוש מעל תוכן העניינים.
