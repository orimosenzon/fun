# Session 2026-03-07 (סשן שני)

## מה עשינו
- הוספת LRClib כשלב ביניים בין כתוביות YouTube ל-Whisper
  - `/api/search?q={title}` — חיפוש לפי כותרת הוידאו
  - פרסינג של פורמט LRC לסגמנטים (ברמת שורה, לא מילה)
  - זרימה חדשה: YouTube captions → LRClib → Whisper
- הוספת Dockerfile לפריסה בענן:
  - Python + Node.js + FFmpeg
  - pre-download של מודל Whisper base
  - gunicorn עם timeout 300
- הוספת .dockerignore
- הוספת gunicorn ל-requirements.txt
- ניסיון לפרוס ב-Railway — נתקענו על אישור כפול של GitHub (Passkey לטלפון לא עבד)

## מצב נוכחי
- הקוד מוכן לחלוטין לפריסה (Dockerfile, requirements, קוד)
- Railway לא חובר עדיין — ה-GitHub App לא הותקן בגלל בעיית אימות
- השרת המקומי רץ על port 5001

## צעדים הבאים
1. **המשך פריסה ב-Railway** — לנסות שוב את Configure GitHub App, הפעם עם שיטת אימות אחרת (לא Passkey)
   - אפשר לנסות: SMS / קוד מאימייל / סיסמה
2. לאחר חיבור — הגדרת Root Directory ל: `vibe_coding/karaoke`
3. Generate Domain לקבלת כתובת ציבורית
4. בדיקה מקצה לקצה עם שיר פופולרי
