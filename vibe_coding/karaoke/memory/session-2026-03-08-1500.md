# Session 2026-03-08 (אחה"צ)

## מה עשינו
- עברנו מהורדת MP3 (yt-dlp) ל-YouTube IFrame API
  - הסרנו `_download_audio` לחלוטין מ-transcriber.py
  - הסרנו route `/audio/<filename>` מ-app.py
  - הפרונטאנד עכשיו מפעיל YouTube Player מוטמע במקום `<audio>`
  - סנכרון מילים: `setInterval` 100ms + `getCurrentTime()` במקום `timeupdate`
  - הפרונטאנד שולח `title` ב-`/api/process` לשימוש ב-LRClib
- פרסום בפייסבוק — תגובות חיוביות מהמשתמשים!

## בעיות פתוחות
- **סנכרון לא מדויק בחלק מהשירים** — משתמשים דיווחו שהמילים לא תמיד מסונכרנות טוב עם הזמן
  - ייתכן שהבעיה ב-YouTube captions (טיימינג לא מדויק)
  - ייתכן שהבעיה ב-LRClib (offset קבוע בין המוזיקה לטקסט)
  - ייתכן שצריך לאפשר למשתמש לכוון offset ידנית (+/- שניות)

## מצב נוכחי
- הפרויקט חי ב-`fun-production-d221.up.railway.app`
- פעיל ומשתמשים משתמשים בו

## צעדים הבאים
1. לחקור את בעיית הסנכרון — לבדוק שיר ספציפי שלא עובד ולהשוות מקור (captions vs LRClib)
2. אפשרות: כפתורי כיוון offset (+/- 0.5s) בממשק
3. אפשרות: להציג מקור המילים (captions/lrclib) כדי לדבג
