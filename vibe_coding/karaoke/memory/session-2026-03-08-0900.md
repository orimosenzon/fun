# Session 2026-03-08

## מה עשינו
- הסרנו Whisper לחלוטין (openai-whisper/torch ~2GB גרם ל-build timeout ב-Railway)
  - הסרת `openai-whisper` מ-requirements.txt
  - הסרת `import whisper`, `_model`, `get_model()`, `_whisper_transcribe()` מ-transcriber.py
  - כשאין כתוביות ואין LRClib — מחזיר שגיאה ברורה
- פריסה ב-Railway הצליחה:
  - כתובת: `fun-production-d221.up.railway.app`
  - נדרש להגדיר `PORT=8000` ב-Variables (Railway מגדיר PORT=8080 כברירת מחדל)
- שיפור הודעת שגיאה — מוצגת באדום וגדולה כשאין מילים
- שינוי ממשק: במקום הדבקת URL — חיפוש לפי שם שיר
  - `/api/search?q=...` — חיפוש ביוטיוב + סינון לפי LRClib (parallel threads)
  - תוצאות מוצגות כ-grid של כרטיסיות עם thumbnail ושם
  - לחיצה על שיר → מנגן עם מילים

## בעיות פתוחות
- **אודיו 404**: הקובץ `/audio/{id}.mp3` מחזיר 404 — הורדת האודיו כנראה נכשלת בשקט ב-Railway
  - yt-dlp ייתכן שנחסם על ידי YouTube ב-IP של Railway
  - צריך לבדוק לוגים של Railway לאחר ניסיון השמעה
  - הלוגים הראו `GET /audio/2012bf508b3f.mp3 404 (Not Found)`

## מצב נוכחי
- הממשק החדש (חיפוש) עלה ל-Railway ועדיין בבנייה
- כתובת: `fun-production-d221.up.railway.app`

## צעדים הבאים
1. לבדוק שהממשק החדש עובד (חיפוש + תוצאות + כרטיסיות)
2. לפתור בעיית אודיו 404 — לבדוק Railway Deploy Logs אחרי ניסיון השמעה
   - ייתכן שצריך להוסיף logging מפורט ב-_download_audio
   - ייתכן שצריך בדיקת קיום הקובץ אחרי הורדה
3. לשקול: האם להפוך את הפלטפורמה ל-paid plan כדי לקבל persistent storage
