# דרך קיצור

אפליקציית ווב ליוזמת שבילי ההליכה של פרדס חנה-כרכור, מעל שכבת ה-Google My Maps של היוזמה.

המפה נשארת מקום העריכה. האפליקציה היא שכבת התצוגה מעליה: רשימה שאפשר לסרוק ולחפש בה,
תמונות שנפתחות בגודל מלא, וסטריט ויו לכל כניסה לשביל.

## מבנה

```
build_data.py        שולף את המפה החיה, ממרר תמונות, וכותב web/data/trails.json
build_network.py     ממיר את קובצי ה-GIS של תוכנית רשת הרכיבה ל-web/data/layers.json
kitzur_sheet.py      מפיק גיליון עבודה (xlsx) מאותה מפה
network_src/         קובצי המקור של תוכנית הרכיבה (shapefiles מכווצים)
web/                 האפליקציה. סטטית לגמרי, בלי build ובלי שרת
  index.html
  app.css
  app.js             מפה, רשימה, פרטים, ניווט
  layers.js          מרשם השכבות וחלון ההדלקה/כיבוי
  draft.js           הוספת שביל: הקלטה בהליכה, ציור, אחסון וייצוא
  data/trails.json   נוצר, לא נערך ידנית
  data/layers.json   נוצר, לא נערך ידנית
  img/               תמונות ממוררות, נוצרות
```

## הפעלה

```bash
pip install requests openpyxl pillow pyshp pyproj

python3 build_data.py          # רענון נתונים ותמונות מהמפה
python3 build_network.py       # רענון שכבות רשת הרכיבה
cd web && python3 -m http.server 8000
```

ואז לפתוח `http://localhost:8000`. חייבים שרת ולא פתיחת קובץ ישירות, כי האפליקציה
טוענת את הנתונים ב-fetch.

`build_data.py --no-photos` מרענן רק את הנתונים ומדלג על התמונות, וזה מהיר בהרבה
כשמשנים קוד.

## פריסה

הכל סטטי, ולכן GitHub Pages מספיק:

```
https://orimosenzon.github.io/fun/vibe_coding/dereh_kitzur/
```

אין צעד בנייה ואין מה להגדיר. איתור מיקום עובד רק מעל https, ו-Pages מספקת https.

## שלושה דברים שכדאי לדעת לפני שנוגעים בקוד

**התמונות ממוררות, ולא במקרה.** גוגל מגישה את תמונות ה-My Maps עם
`cross-origin-resource-policy: same-site`, ולכן דפדפן מסרב להציג אותן מכל דומיין אחר.
`curl` דווקא מוריד אותן בהצלחה, וזו בדיוק המלכודת.

**שמות הקבצים הם hash של תוכן התמונה.** גוגל חותמת מחדש כל כתובת תמונה בכל שליפת KML,
כך ששתי בניות רצופות לא רואות אף כתובת משותפת. מיפוי לפי URL היה מכפיל את התיקייה בכל
הרצה. המחיר הוא שכל בנייה מורידה הכל מחדש, בערך שלוש דקות.

**אין מפתח API בשום מקום.** רקע המפה מגיע מ-OpenStreetMap ומ-Esri, סטריט ויו נפתח
בקישור לגוגל מפות במקום ווידג'ט בתשלום, ומיקומי המצלמה מחושבים מנתוני כבישים של
OpenStreetMap דרך Overpass.

## שכבות

המפה מציגה ארבע שכבות, ומה שדולק נכנס גם לרשימה ולחיפוש:

| שכבה | מקור | ברירת מחדל |
|---|---|---|
| שבילי היוזמה | `data/trails.json`, מהמפה החיה | דלוקה |
| רשת רכיבה קיימת | `network_src/`, תוכנית המושבה | כבויה |
| רשת רכיבה מוצעת | אותו מקור, קו מקווקו | כבויה |
| הטיוטות שלי | IndexedDB בדפדפן | דלוקה |

מצב ההדלקה נשמר ב-localStorage לכל מבקר בנפרד.

## הוספת שביל, והדרך חזרה ל-My Maps

**לגוגל אין API לכתיבה ל-My Maps.** Map Management API הוא לעיצוב מפות ענן, ו-Drive API
רואה את קובץ המפה אבל לא את תוכנו. לכן שביל חדש לא יכול להיכתב למפה מהאפליקציה, וזו
הסיבה שהמסלול עובר דרך קובץ.

השרשרת המלאה:

1. **הקלטה בהליכה** או **ציור על המפה**, מכפתור "שביל חדש" ברשימה.
2. הטיוטה נשמרת ב-IndexedDB במכשיר, עם תמונות מוקטנות. שום דבר לא נשלח לשום מקום.
3. **ייצוא KML** ושליחה דרך תפריט השיתוף של הטלפון, כלומר בוואטסאפ ישירות ליואב.
4. ייבוא הקובץ ל-My Maps מייצר שם **שכבה חדשה** (אי אפשר למזג לשכבה קיימת, והתקרה
   היא עשר שכבות למפה).
5. ההרצה הבאה של `build_data.py` מושכת את השביל בחזרה כשביל רגיל, והאפליקציה מסמנת
   שהטיוטה כבר נקלטה כדי שאפשר יהיה למחוק אותה.

הקלטה בהליכה מסננת מדידות עם דיוק גרוע מ-30 מ׳ ותזוזות קטנות מ-4 מ׳. בלי הסינון
הרעש של ה-GPS מעקם את השביל יותר מרוחב השביל עצמו, והעיוות הזה נשמר לתמיד.

## מה האפליקציה נותנת מעבר ל-My Maps

- רשימה ממוינת (לפי אורך, קרבה אליך, או כמות תמונות) עם חיפוש חופשי
- תמונות בגודל מלא עם החלקה בין תמונות, ולא תצוגה מוקטנת בתוך בועית
- **סטריט ויו לשתי הכניסות של כל שביל.** המצלמה לא עומדת על השביל אלא על הכביש הקרוב
  ביותר, ומביטה אל פתח השביל. זה החלק שבאמת קשה במפה המקורית: לזהות את הפתח בשטח.
  כניסות שרחוקות מכל כביש מסומנות בקו מקווקו, כי ייתכן שאין להן צילום 360
- מיקום נוכחי ומיון "הקרובים אליי"
- החלפה בין תצוגת רחובות ללוויין

## המשך

תמיכה בעריכה והוספת מסלולים מתוך האפליקציה. פרטים ופתחים פתוחים: `memory/project-knowledge.md`.
