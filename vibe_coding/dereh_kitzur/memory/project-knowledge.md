# דרך קיצור - ידע פרויקט

## מה זה
יוזמה קהילתית בפרדס חנה-כרכור למיפוי ובניית רשת שבילי הליכה ("קיצורי דרך") בתוך המושבה.
היוזם: יואב טריפון.

הפרויקט הזה הוא צד התוכנה של היוזמה. **מאז 22/8/2026 הוא גם מסד הנתונים שלה**, ולא
עוד תצוגה מעל שכבת My Maps. פרטים בסעיף "היפוך הכיוון".

## חי באוויר
https://orimosenzon.github.io/fun/vibe_coding/dereh_kitzur/
(פורס 15/8/2026 מ-GitHub Pages של ריפו `fun`, ענף master. אין צעד בנייה.)

**הנתונים לא יושבים בריפו הקוד** אלא ב-`orimosenzon/derech-kitzur-data`. פרסום שביל
לא דורש פריסה מחדש של האפליקציה.

## מקורות
- ריפו הנתונים: https://github.com/orimosenzon/derech-kitzur-data
- מפת My Maps (mid): `19q4SgSScTh3pW3lRqQO9dQ8kw8z2wTg` (כיום יעד לייצוא, לא מקור)
- KML: `https://www.google.com/maps/d/kml?mid=<mid>&forcekml=1`
- ערך ויקי: https://pardespedia.info/wiki/דרך_קיצור (נוצר 7/2026 בסשן של pardespedia)
- הערך של היוזם: https://pardespedia.info/wiki/יואב_טריפון

## מלכודת שכבר נפלנו בה: התמונות לא ניתנות ל-hotlink

התמונות של My Maps יושבות ב-`mymaps.usercontent.google.com` ומוגשות עם הכותר
`cross-origin-resource-policy: same-site`. המשמעות: **curl מוריד אותן מצוין, ודפדפן מסרב
להציג אותן** מכל דומיין אחר. השגיאה בקונסול היא `ERR_BLOCKED_BY_RESPONSE.NotSameSite`.

לכן `build_data.py` **ממרר את התמונות מקומית** ל-`web/img/` ומקודד אותן מחדש ל-webp.
אל תנסה לחזור ל-hotlink כי "זה עובד ב-curl". זה לא יעבוד באפליקציה.

בדיקה עם curl בלבד לא תתפוס את זה. חייבים רינדור בדפדפן אמיתי.

## מלכודת שנייה: כתובות התמונות מתחלפות בכל שליפה

גוגל חותמת מחדש כל כתובת תמונה בכל בקשת KML. נבדק: שתי שליפות בהפרש דקות החזירו
**אפס כתובות משותפות** מתוך 84. לכן אי אפשר לזהות תמונה לפי ה-URL שלה.

הפתרון: שמות הקבצים ב-`web/img/` הם hash של **תוכן** התמונה. כך כל בנייה מחדש מייצרת
בדיוק אותם שמות, בלי כפילויות ובלי רעש ב-git. בנייה שנייה בטעות לפי URL יצרה 336 קבצים
ו-82MB במקום 168 ו-41MB. `prune()` מוחק קבצים שאף שביל כבר לא מפנה אליהם.

המחיר: כל בנייה מורידה מחדש את כל התמונות (כשלוש דקות). רק הקידוד נחסך.

## בדיקת סטריט ויו לא ניתנת לאוטומציה מכאן

ניסיון לוודא כיסוי 360 דרך דפדפן headless **לא עובד**: גוגל מחזירה "אין תמונות של
Street View" גם בכיכר טיימס. זו התנהגות מול דפדפן אוטומטי, לא חוסר כיסוי אמיתי.
אל תסיק מזה שאין כיסוי. אימות אמיתי דורש מפתח Maps (נקודת ה-metadata עצמה חינמית),
או פשוט לחיצה ידנית על קישור.

## מה קיים בקוד
- `build_data.py` - היה הצינור המרכזי, כיום כלי ייבוא בלבד. `--seed` מרענן את העותק
  הלא מקוון, `--from-mymaps` מושך לקובץ נפרד. הרצה בלי דגל נעצרת בכוונה.
- `export_mymaps.py` - מייצר KML מריפו הנתונים למפה של יואב. **קובץ לכל שכבת שבילים**,
  כי ייבוא ל-My Maps ממילא יוצר שכבה לכל קובץ. שם הקובץ נושא את שם השכבה בעברית, כדי
  שמי שמייבא ידע איזה קובץ שייך לאיזו שכבה.
- `build_network.py` - ממיר את קובצי ה-GIS של תוכנית רשת הרכיבה ל-`web/data/layers.json`.
- `build_places.py` - מושך ערכי מקומות מפרדספדיה ל-`web/data/places.json`.
- `web/` - האפליקציה. סטטית לחלוטין, בלי build ובלי מפתח שירות. `store.js` (קריאה
  וכתיבה לריפו הנתונים), `layers.js` (מרשם שכבות), `draft.js` (הוספת שביל, ייבוא,
  ייצוא), `app.js` (מפה, רשימה, פרטים, ניווט, נעיצת מקומות),
  `vendor/` (תוסף הכיווניות של MapLibre).
- `kitzur_sheet.py` - מסלול נפרד שמפיק xlsx מאותה מפה (מקטעים / נקודות ציון / סיכום).
- `derech_kitzur_map_data.xlsx` - הפלט האחרון של הגיליון.

תלויות: `requests`, `openpyxl`, `pillow`, `pyshp`, `pyproj`.

## שכבות (נוסף 22/8/2026, הורחב 23/8/2026)

מרשם אחד ב-`layers.js`. מה שדלוק נכנס למפה **ולרשימה ולחיפוש** גם יחד, כי רשימה
שמציעה שביל שלא רואים על המפה מבלבלת. ההעדפה נשמרת ב-localStorage.

| שכבה | kind | מקור | ברירת מחדל |
|---|---|---|---|
| דרכי קיצור | trails | `data/trails.json` | דלוקה |
| מקומות של דרך קיצור | waypoints | `trails.json` → `waypoints[]` | דלוקה |
| שביל סובב פרכו"ר | network | `import_offroad.py` | דלוקה |
| שכבות שבילים של עורך | trails | `trails.json` → `layers[]` | כבויה מ-10/9/2026 |
| שבילי אופניים קיימים | network | shapefile, 31 מקטעים, 19.5 ק"מ | כבויה |
| שבילי אופניים מתוכננים | network | shapefile, 48 מקטעים, 34.5 ק"מ | כבויה |
| מקומות מפרדספדיה | places | `data/places.json`, 454 מקומות | כבויה |
| הטיוטות שלי | drafts | IndexedDB | דלוקה |

**שכבות שבילים של עורך (23/8/2026).** עד אז כל שביל שפורסם נכנס לשכבה אחת קשיחה.
עכשיו ל-`trails.json` יש מערך `layers`, ולמקטע יש שדה `layer` אופציונלי. הבחירה
לשמור את שתיהן **באותו קובץ** מכוונת: יצירת שכבה והכנסת שביל אליה הן עריכה אחת
לאותו מסמך, עם אותו מנגנון retry, ואין קובץ שני שיכול לצאת מסנכרון.
- שביל שמצביע על שכבה שנמחקה חוזר אוטומטית לשכבת "דרכי קיצור" (`home()` ב-layers.js),
  ולא נעלם.
- `removeLayer` מנקה את השדה מכל המקטעים באותו commit. מחיקת שכבה לא מוחקת שבילים.
- `resetTrails` ולא `refresh` אחרי כתיבה, כי כתיבה יכולה לשנות **איזו שכבה מחזיקה מה**
  ולא רק את הגיאומטריה בתוך שכבה.

**מקור נתוני הרכיבה:** אורי קיבל שני zip של shapefiles מתוכנית רשת הרכיבה של המושבה
(חברת ההנדסה IOA, פרויקט 1102). מכווצים ב-`network_src/`, 11KB בסך הכל.
- "מוצעת" נמצאת ב**רשת ישראל החדשה** (ITM, EPSG:2039) וחייבת היטל. "קיימת" כבר ב-WGS84.
  אל תניח שהשתיים חולקות מערכת קואורדינטות.
- קידוד ה-DBF שונה בין השתיים: cp1255 מול UTF-8. ה-`.cpg` אומר איזה.
- שמות שדות ב-DBF חתוכים לעשרה בתים, והבית האחרון בעברית לרוב מתקלקל. `alias()`
  מזהה לפי תחילית קריאה במקום לפי התאמה מלאה.
- **למקטעים אין שמות במקור, רק מספר סידורי.** `build_network.py` מצמיד לכל מקטע את
  שמות הרחובות שהוא עובר לאורכם, מ-Overpass, בדגימה כל 40 מ׳. כל 79 המקטעים קיבלו שם.
  זה ההבדל בין "מקטע 17" ל"רשת משנית · דרך הבנים", וגם מה שגורם לחיפוש רחוב לעבוד.
- Overpass מחזיר **406** לבקשה בלי User-Agent. חייבים כותרת.

## היפוך הכיוון: האפליקציה היא המסד (22/8/2026)

**ההחלטה המרכזית של הפרויקט.** עד אז My Maps היה מקור האמת. אורי החליט להפוך:
הנתונים עוברים לריפו משלהם, האפליקציה כותבת אליהם, ו-My Maps מקבל עותק מיוצא.

```
ריפו נתונים ציבורי: orimosenzon/derech-kitzur-data
  data/trails.json   מקור האמת
  data/layers.json   שכבות רשת הרכיבה
  img/               תמונות
```

- קריאה: `raw.githubusercontent.com`, בלי אימות, עם CORS פתוח. **מטמון 5 דקות.**
- כתיבה: GitHub contents API עם מפתח fine-grained של העורך, מוגבל לריפו הזה בלבד,
  `Contents: Read and write`, ב-localStorage.
- הריפו נפרד מ-`fun` בכוונה. `fun` ציבורי ומכיל את כל הפרויקטים; מפתח שידלוף מטלפון
  היה יכול לדרוס הכול. עכשיו הוא נוגע רק בנתוני שבילים ציבוריים, וכל כתיבה היא commit.

**מלכודת שנפלנו בה: `Cache-Control` מפיל בקשות ל-API של גיטהאב.** הכותרת לא נמצאת
ב-`Access-Control-Allow-Headers`, אז קדם-הבדיקה של CORS נכשלת והבקשה לא יוצאת בכלל.
השגיאה שמתקבלת היא `Failed to fetch` בלבד, בלי רמז. הפתרון: פרמטר `?t=` בכתובת.

**מלכודת שנייה: אחרי פרסום, `raw` מחזיר עדיין את הגרסה הישנה** למשך עד 5 דקות, ולכן
העורך היה רואה את השביל שלו נעלם. שני תיקונים: פעולת כתיבה מחזירה את המסמך שנכתב
והאפליקציה משתמשת בו ישירות, ובנוסף עורך קורא דרך ה-API (טרי תמיד) ולא דרך raw.

**תור האישורים רץ על וואטסאפ ולא על שרת** — היה נכון עד 25/8/2026. השרתון נדחה אז
"עד שהנפח יצדיק אותו", וזה היה השיקול הלא נכון: מה שהצדיק אותו בסוף היה חיכוך ולא
נפח. ראה "המפתח נעלם" למטה. ייבוא קובץ KML/GPX נשאר כמסלול נוסף.

**`build_data.py` כבר לא יוצר את trails.json.** הרצה רגילה נעצרת עם הודעה, כי היא
הייתה דורסת שבילים שנוספו מהאפליקציה. `--seed` מרענן את העותק הלא מקוון מריפו הנתונים,
`--from-mymaps` מושך לקובץ נפרד לבדיקה.

**`export_mymaps.py`** מייצר את ה-KML למפה של יואב. שים לב לשתי סבבי בריחה בתיאור:
טקסט המשתמש נבלם כ-HTML, וכל ה-HTML נבלם שוב כ-XML. `<br>` גולמי מפיל את הקובץ.

## הוספת שבילים (נוסף 22/8/2026)

**לגוגל אין API לכתיבה ל-My Maps** ולא היה כזה מעולם. Map Management API הוא לעיצוב
מפות ענן, ו-Drive API רואה את קובץ המפה (`application/vnd.google-apps.map`) אבל לא את
תוכנו. אל תחפש שוב. זו הסיבה להיפוך הכיוון שמתואר למעלה.

- **הקלטה בהליכה היא הדרך העיקרית ולא הציור.** 83% מהשבילים לא קיימים בשום מפה, אז
  אין מה לשרטט מעליו. הסינון: מדידה עם דיוק גרוע מ-30 מ׳ נזרקת, ותזוזה קטנה מ-4 מ׳
  נחשבת עמידה במקום. בלי זה רעש ה-GPS מעקם את התוואי יותר מרוחב השביל.
- ייבוא KML ל-My Maps תמיד יוצר **שכבה חדשה**, אי אפשר למזג לשכבה קיימת, והתקרה היא
  עשר שכבות למפה. `export_mymaps.py` מייצא את כל הסט, ולכן מחליפים שכבה שלמה.
- אוטומציית Playwright מול ממשק My Maps נשקלה ונדחתה, ואחרי היפוך הכיוון היא כמעט
  מיותרת: היא הייתה חוסכת ייבוא ידני של עותק לצפייה, לא של מקור אמת.
- תמונות נשמרות כ-Blob ב-IndexedDB אחרי הקטנה ל-1600px ו-webp. KML לא נושא תמונות,
  אז הן נשארות מקומיות ומשמשות את מי שמיפה.

## ההזמנה: המפה מבקשת שיוסיפו לה (10/9/2026)

הפתיחות הייתה קיימת מ-28/8/2026 ולא ניכרה על המסך. הכניסה היחידה להוספת שביל הייתה
שבב "+ שביל חדש" בין שבבי המיון, כלומר במקום שנקרא כמסנן, ומי שנכנס פגש מוצר גמור.
אורי ביקש שכל מי שמגיע יוזמן במפורש, בלי סיסמה, עם שם שיהיה הקרדיט שלו.

- **כפתור קבוע** בראש הרשימה (`#add`, מחלקה `.invite`), רוחב מלא בצבע ההדגשה, מעל
  החיפוש כדי שיהיה על המסך גם כשהפאנל בגובה המינימלי. השבב הישן הוסר.
- **חלון פתיחה בביקור ראשון**, `#welcome-sheet`, פעם אחת לדפדפן (`dk.welcome.v1`).
  אותו אלמנט משמש גם להודעה על מה שירד מהתור, ושתי ההודעות לא יכולות להתנגש כי
  השנייה מחייבת שליחה קודמת.
  - **הכותרת:** "את המפה הזאת בנו תושבים". הנוסח הראשון היה "המפה הזאת נבנית
    מהתושבים", ואורי תפס שזה נשמע כמו דרך רומית מגולגלות האויב. הוא צדק, וזה דקדוק:
    "נבנה מ-X" אומר מאיזה **חומר** משהו עשוי. גם השמטת ה' הידיעה מכוונת - "תושבים"
    אומר אנשים כמוך, "התושבים" אומר גוף שאתה לא חלק ממנו.
- **שם חובה בשליחה.** `Store.setName` הופרד מ-`Store.enable`: שם הוא קרדיט ולא
  הרשאה, ו-`enable` היה מחזיר false לתושב בלי סיסמה, כלומר מסרב לו על דלת שהוא לא
  דפק בה. נשמר ב-`dk.name.v1`, כך שהשביל השני לא נשאל שוב.

**מה ששלחת נשאר לעיניך.** עד אז השליחה בלעה את הטיוטה, הציגה `alert` ונעלמה, וזה
נראה בדיוק כמו אובדן. עכשיו `dk.sent.v1` מחזיק את המזהים ששלחת (עשרים אחרונים):
- שכבת הממתינים נטענת גם למי שאינו עורך, מסוננת לפריטים שלו בלבד ובשם "מה ששלחת".
  `pendingShown` מציג אותה לעורך תמיד ולכל אחד אחר רק כשיש בה משהו שלו.
- מסך הפריט הממתין מתפצל לשניים: העורך רואה אשר/דחה, השולח רואה "ממתין לאישור" ואת
  האפשרות לצרף תמונות וסרטון **עכשיו**. `pending.json` ו-`img/` הם שני הנתיבים
  הפתוחים בשרתון, ולכן זה עובד בלי סיסמה. רק מדיה מתווספת, לא התוואי ולא השם, כי
  אלה בדיוק מה שנמצא בבדיקה.
- כשפריט יורד מהתור, `settle()` אומר פעם אחת מה קרה לו: "אושר" רק אם יש שביל בשם
  הזה בשכבות המפורסמות, אחרת "כבר לא בתור". **לא מנחשים אישור מעצם ההיעלמות**, אחרת
  בסוף מברכים מישהו על שביל שנדחה.

**מלכודת: ההזמנה הצליחה והרשימה בלעה את מה שנוסף.** אורי צייר שני קיצורים ודיווח
שהם "די נאלמו". הם היו שם. **המיון קבר אותם:** ברירת המחדל היא "הארוכים", וקיצור דרך
קצר בהגדרה, אז טיוטה של 200 מ׳ נחתה בשורה 60 מתוך 75; ובמיון "עם תמונות" היא נעלמה
לגמרי כי אין לה תמונות. זה היה נכון גם קודם, אבל רק עכשיו זה קרה למישהו, כי עכשיו
מזמינים אותו להוסיף. התיקון ב-`items()`: **טיוטות והגשות שלך צפות מעל כל מיון**, והשורה
אומרת "שמור אצלך, עוד לא נשלח" או "ממתין לאישור". "עם תמונות" עדיין מסנן טיוטה בלי
תמונות, כי זה סינון שהמשתמש ביקש במפורש.

**שמור ≠ נשלח, ו"שמרתי" נשמע כמו "שלחתי".** אורי צייר, שמר, ולא שלח, ולא במקרה:
**דף הטיוטה נפתח ב"הגעה · נווט אליי לכאן"** — ניווט לשביל שהוא הלך בו לפני חמש שניות —
ו"שלח ליוזמה" ישב באמצע הדף באותו משקל ויזואלי כמו "ייצוא GPX". ארבעה תיקונים:
1. **באנר `.draft-state`** בראש הדף, בסגול של הטיוטות: "עוד לא נשלח, שמור אצלך בלבד,
   אף אחד חוץ ממך לא רואה אותו". המילה "טיוטה" לבדה לא מספיקה — מעבד תמלילים לימד
   את כולם שטיוטה היא משהו שנשמר.
2. **`.send-cta`** — רוחב מלא, ירוק מלא, הדבר הממולא היחיד בדף.
3. **`detailExtras(seg, extras)`** מקבל את בלוק ההגעה כפרמטר ושותל אותו **מתחת**.
   קודם app.js הדפיס אותו מעל, וזו הייתה רוב הסיבה שהשליחה לא נראתה.
4. **פס `#unsent`** בראש הרשימה כשיש משהו שלא נשלח. כשהוא מופיע כרטיס ההזמנה מקבל
   `.compact` ומאבד את שורת ההסבר, אחרת שני כרטיסים אוכלים פאנל של 45vh.

התג בשורה של טיוטה הוא **"לא נשלח"** ולא "טיוטות" (`short` בשכבה): תג אומר מצב, לא
ארון תיוק.

**מלכודת שנסגרה בדרך: `Store.queue()` בלעה שגיאות רשת והחזירה תור ריק.** כל עוד רק
עורך קרא את התור זה היה חסר משמעות. ברגע שיש דיווח למשתמש על מה שירד מהתור, שרתון
מנותק היה מודיע לכל מי ששלח ששבילו ירד. עכשיו היא זורקת; רק "אין שרתון בכלל" מחזיר
תור ריק, כי אז באמת אין תור.

**סרטון על טיוטה** נשמר כ-`{yt, thumb}` בשדה `videos` ברשומת ה-IndexedDB, בנפרד
מ-`photos` שהוא רשימת Blob, כדי שכל מי שעובר על `rec.photos` לא יצטרך לשאול איזה סוג
פריט הוא מחזיק. מצטרף לגלריה ב-`toSegment` ונוסע עם השביל גם בשליחה וגם בפרסום.

**תיקון אגב:** למסך של פריט ממתין לא היה `#pub-msg`, ולכן שגיאות באישור ובדחייה נבלעו
בשקט ב-`detailSay` (שהוא no-op כשאין אלמנט). נוסף לשני הענפים.

**ברירת המחדל של השכבות צומצמה לשלוש:** דרכי קיצור, מקומות של דרך קיצור, שביל סובב
פרכו"ר. שכבות שבילים שעורך יצר כבויות מעכשיו. `PREF` הועלה ל-`dk.layers.v3`, אחרת
מבקר ותיק נושא `true` מפורש ולא רואה את השינוי.

## שכבה מוסתרת: `private: true` על שכבת עורך (12/9/2026)

אורי יצר ב-11/9 שכבה "דרכים שאולי צריך לפרוץ" (`tl-mtx2fqjp`), הוסיף לה שביל
"הנשיא - עמק השיטים" (דרך שהופקעה ותושבים סיפחו לחלקתם) ומחק אותו באותו ערב, כי מפה
ציבורית שאומרת לבעל הגדר "את זה צריך לפתוח" מתחילה ריב לפני שהמיפוי נגמר. הבקשה:
השכבה גלויה רק למי שנכנס עם סיסמה.

- **המנגנון הוא בדיוק השער של תור האישורים**, שכבר הסתיר שכבה שלמה ממי שאינו עורך.
  ב-`layers.js`: `privateHidden(l)`, `hidden(l)`, `shown(l)`. כל מה שקורא
  `visible()`, `applyVisibility`, `inSheet`, `renderLegend` ו-`shareable` עובר דרכו.
  ב-`app.js`: `select()` מסרב ל-`?sel=` על שביל מוסתר, `repaint()` בודק `shown` ולא
  `on`, ו-`editorChanged()` מריץ `applyVisibility` בשלוש נקודות המעבר (resume, כניסה
  עם סיסמה, כיבוי). **בלי הקריאה המפורשת הרשימה הייתה מורידה את השביל בכיבוי מצב
  עריכה והקו היה נשאר על המפה**, כי את שכבות ה-GL מחליף רק `applyVisibility`.
- **השביל שוחזר מההיסטוריה של ריפו הנתונים** (הועבר מ-`debeb23`, לפני המחיקה
  `b4f7e4c`), הפעם עם `layer: tl-mtx2fqjp`. במקור הוא לא היה משויך לשכבה בכלל, כלומר
  ישב ב"דרכי קיצור" הציבורית. התמונות (`9b23444c21018a`) לא נמחקו מעולם.
- **סדר הפריסה חשוב:** קודם הקוד לאוויר, אחר כך הנתונים. בסדר ההפוך השביל היה גלוי
  לכולם עד שהאפליקציה מתעדכנת, ודף ב-GitHub Pages נשמר במטמון עד עשר דקות.
- **שכבה מוסתרת עם שבילים לא נמחקת** (הכפתור מנוטרל ומסביר), כי `removeLayer` מחזיר
  את השבילים ל"דרכי קיצור" ומפרסם אותם. הסרת הדגל משכבה מאוכלסת מבקשת אישור.
- `export_mymaps.py` מדלג על שכבה מוסתרת ועל שביליה ועל טיול שעובר בהם; `build_og.py`
  לא סופר אותם.
- **מה זה לא:** ריפו הנתונים ציבורי וה-JSON הגולמי מכיל את השכבה, וגם ההיסטוריה של
  git מכילה את השביל מאז 11/9 בכל מקרה. הסתרה אמיתית = ריפו פרטי נוסף + הרחבת מפתח
  השרתון + ניתוב ב-worker וב-store.js. הוצע לאורי ולא נעשה.
- בדיקה: `scratchpad/serve.py` (שרת מקומי שמדמה את השרתון עם סיסמה `secret` וכותב
  לשיבוט של ריפו הנתונים) + `test_private.py` ב-Playwright על כרום אמיתי: מבקר,
  עורך, כיבוי מצב עריכה באמצע צפייה בשביל, יצירה ומחיקה של שכבה מוסתרת דרך הטופס.

## מלכודת: הרשימה הייתה מתה עד שסגנון המפה נטען
עד 22/8/2026 `boot()` עשה `await` על `map.once('load')` **לפני** שרינדר את הרשימה וחיבר
את הכפתורים. עם קליטה חלשה כל הפאנל היה מת, וזה בדיוק המצב בשטח. עכשיו הרשימה,
החיפוש והכפתורים עולים ברגע שה-JSON הגיע, והמפה מצטרפת אחר כך.

## החלטות עיצוב באפליקציה
- **בסיס המפה הוא MapLibre GL ולא גוגל**, כי גוגל מפות דורשת מפתח וחשבון חיוב.
  הועבר מ-Leaflet ב-15/8/2026 כדי לאפשר הטיית מבט. נקודת ההחלפה היא `BASEMAPS` בראש
  `app.js`. אריחים וקטוריים מ-OpenFreeMap (חינם, בלי מפתח), לוויין מ-Esri.
- **הטייה ותבליט:** כפתור 3D מטה ל-58 מעלות, ויש DEM מ-AWS terrarium עם הגזמה 2.5.
  התבליט האמיתי כאן זעיר (46 מ׳ על פני כל המושבה, 4 מ׳ לאורך שביל טיפוסי), אז ההטייה
  עצמה נותנת את רוב האפקט ולא הגובה.
- **בניינים תלת-ממדיים מכובים בכוונה.** רק לבניין אחד מתוך 400 שנבדקו יש נתון גובה
  ב-OSM, אז כולם היו בולטים באותו גובה ברירת מחדל. `applyOverlays` מסתיר כל שכבת
  `fill-extrusion`.
- **נקודות ציון הן HTML markers ולא שכבת symbol**, כדי לא להיות תלויים בכך שערכת
  הגליפים של הסגנון מכילה עברית.
- **סטריט ויו נפתח בקישור** לסכמת ה-URL הרשמית של גוגל (`map_action=pano`), שהיא חינמית
  לגמרי ובנייד פותחת את אפליקציית גוגל מפות. ווידג'ט מוטמע היה מחייב מפתח בתשלום.
- **סטריט ויו לשתי הכניסות של כל שביל, עם heading פנימה.** השביל עצמו כמעט אף פעם לא
  מצולם בסטריט ויו, אבל הרחובות בקצוות כן. הכניסה היא בדיוק החלק הקשה לזיהוי בשטח,
  ולכן זו הפעולה המרכזית במסך הפרטים ולא תוספת.
- 41MB של תמונות ב-`web/img/`. הטעינה בפועל קלה כי הרשימה מושכת רק תמונות ממוזערות
  (17–60KB), והתמונה המלאה (444KB בממוצע) נטענת רק בלחיצה.
- **טור הכפתורים הצפים מחולק לשלוש קבוצות** (9/9/2026), קריאה מלמטה למעלה: "איפה אני"
  לבד בתחתית כי הוא הכי נלחץ והכי קרוב לאגודל, מרווח 24 פיקסלים, שלישייה צמודה
  (48 פיקסלים) של רקע/הטיה/שכבות שכולם משנים רק את מראה המפה, מרווח 24, ומצב תעופה
  למעלה. מצב התעופה גם ירוק ולא לבן, כי הוא היחיד בטור שיוצא מהמפה במקום לשנות
  אותה. חמישה עיגולים לבנים במרווחים שווים לא אמרו כלום מזה.

## מלכודת: כל מיקום בטור הכפתורים חייב להיכתב מחדש בבלוק המסך הרחב (9/9/2026)
בבלוק `@media (min-width: 860px)` בקובץ `app.css` יש כלל `.fab` שמאפס את כולם
ל-`bottom: 20px`. הוא מופיע בקובץ **אחרי** `.fab-2` עד `.fab-5`, ולמדיה קווארי אין
תוספת ספציפיות, ולכן הוא מנצח כל מיקום שלא נכתב מחדש בתוך הבלוק.

כשנוסף כפתור מצב התעופה (`.fab-5`) הוא לא נכתב שם, אז בדסקטופ הוא נחת בדיוק על
כפתור "איפה אני", ומכיוון שהוא מאוחר יותר ב-HTML הוא בלע את כל הלחיצות. **בלי שום
שגיאה, ובלי שכפתור נעלם מהמסך.** זה נראה כמו החלטת מוצר ולא כמו באג, ולכן שרד סשן
שלם. בטלפון הכל היה תקין, כי שם אין את הבלוק הזה בכלל.

הבדיקה שתופסת את זה: `document.elementFromPoint` במרכז כל כפתור, ולוודא שהוא מחזיר
את הכפתור עצמו. מיקום ורוחב לבד לא מספיקים, כי שני כפתורים במיקום זהה נראים שניהם
תקינים.

## המפתח נעלם: שרתון במקום מפתח לכל עורך (25/8/2026)

**ההחלטה השנייה בחשיבותה בפרויקט, אחרי היפוך הכיוון.** אורי שאל למה בכלל צריך מפתח
וביקש "דרך קלה". התשובה: דף סטטי לא יכול להחזיק סוד, אז או שלכל עורך יש מפתח משלו,
או שיש שרת שמחזיק אחד. עד עכשיו זה היה הראשון, וזה אמר עורך אחד לתמיד.

```
worker/  Cloudflare Worker. מחזיק את מפתח גיטהאב היחיד בפרויקט.
         שתי נקודות קצה: GET /file ו-PUT /file. פרוקסי מצומצם, לא API של המוצר,
         כדי שכל לוגיקת המיזוג וה-retry תישאר ב-store.js איפה שהיא כבר נבדקה.
```

- **שתי דרגות מ-28/8/2026.** להוסיף שביל ולשלוח אותו פתוח לכולם בלי כלום; לאשר,
  לפרסם, למחוק ולסדר מיקומים דורש סיסמה (`EDITOR_KEY` בשרתון, כותרת `X-DK-Key`).
  עד אז לא הייתה הזדהות בכלל וכל מי שמצא את הכתובת יכול היה לשנות את המפה.
  **`pending.json` ו-`img/` נשארו פתוחים בכוונה** — בלעדיהם תושב לא יכול לשלוח.
- **`/health` הוא אורקל סיסמאות ולכן מוגבל בקצב:** עשרה ניחושים שגויים ל-IP
  לחמש דקות. סיסמה נכונה לא צורכת מהמכסה. בלי זה הגידור כמעט חסר ערך.
- **wrangler חייב לרוץ מ-`worker/` עם `--config wrangler.toml`.** הרצה משורש
  הפרויקט מושכת wrangler 4 מהרשת, בונה `wrangler.jsonc` שגוי, ויוצרת שרתון נוסף
  בשם `dereh-kitzur` (בלי c). קרה, ונוקה.
- ההגנות: רשימת נתיבים (רק שלושה קובצי JSON ותמונות webp ששמן hash), בדיקת מבנה
  שמסרבת לרוקן או לנפח מסמך, תקרת 6MB, הגבלת קצב ב-KV, ומתג `READ_ONLY`.
- **"מצב עריכה" הוא מתג ולא הרשאה.** הוא לא מגן על כלום; הוא מרחיק כפתורי מחיקה
  ממי שרק מחפש דרך. השם שמזינים בו לא מאומת ונרשם ב-`by` וב-commit.
- **`WORKER` ב-`store.js` ריק עד שפורסים.** ריק = קריאה בלבד, עם הודעה מסבירה, וכל
  השאר עובד. `window.DK_WORKER` מפנה ל-`wrangler dev` לפיתוח.
- **תור אישורים ב-`data/pending.json`.** תושב שולח בלי כלום, מי שבמצב עריכה רואה
  שכבה `pending` (מוסתרת לגמרי מכל השאר) ומאשר. אישור הוא **שתי כתיבות בסדר הזה**:
  קודם הוספה ל-trails, אחר כך הסרה מהתור. הסדר ההפוך מאבד טיול אם השנייה נכשלת.
- בדיקה: `wrangler dev` מול **ענף זמני** בריפו הנתונים (`--var BRANCH:worker-test`),
  לא מול main. הענף נמחק אחרי. אל תבדוק כתיבה מול main.

## שכבת המקומות מפרדספדיה (נוסף 23/8/2026)

`build_places.py` → `data/places.json`. 454 ערכי מקומות מפרדספדיה, עם תקציר, תמונה,
קישורים חיצוניים, וקישור לערך.

**התמונות מוצגות ישירות מפרדספדיה, בלי מירור.** זה ההפך מ-My Maps, ובכוונה: בדקנו
את הכותרות ואין `Cross-Origin-Resource-Policy`, וה-API של מדיה-ויקי מייצר ממוזערות
(`iiurlwidth`). אין סיבה להעתיק 200 תמונות לריפו.

**המכשול המרכזי: לפרדספדיה אין קואורדינטות בכלל.** אין תוסף GeoData, אין תבנית
מיקום, אין מה לשלוף. גם `insource:` לא עובד כי אין CirrusSearch. ארבעה מקורות מיקום,
לפי סדר אמינות: `manual` (סיכה ידנית, לעולם לא נדרסת) > `osm` (התאמת שם מדויקת) >
`street` (אמצע הרחוב) > `nearby` (מתחם שמוזכר בערך). 261 אותרו, 193 ממתינים לנעיצה.

**ממצא שקובע את התקרה: אף מספר בית במושבה לא נפתר.** כל 90 התוצאות של Nominatim
בתוך התחום היו `class: highway`, כלומר הכביש עצמו — מספרי בתים לא ממופים כאן ב-OSM.
בגרסה ראשונה סימנו 75 מקומות כדיוק `address` וזה היה שקר: תשעה בתי קפה ב"דרך הבנים"
קיבלו אותה נקודה בדיוק. הבדיקה הנכונה היא **סוג התוצאה** ולא סוג השאלה. אל תחזיר את
תווית `address` בלי לבדוק שוב ש-OSM השתפר.

**שתי גרסאות של גזירת מיקום שנפסלו בדרך, שתיהן מייצרות זבל שנראה תקין:**
1. Nominatim בטקסט חופשי על הכתובת כפי שהיא בערך: 2 מתוך 30. פיצול לרחוב+מספר
   ושם עיר: 14 מתוך 30.
2. "הערך מזכיר מקום שכבר אותרנו" בלי הגבלה: 191 עסקים ננעצו על הערך "כרכור" ועוד 52
   על פאב בשם "המקומית", שנתפס בתוך המילים "המועצה המקומית". התיקון: עוגן חייב להיות
   **שם רב-מילי**, לא שכונה, לא שם היישוב, ולא יותר מ-40 טוענים.

**סיכות על אותה נקודה בדיוק מפוזרות על טבעת** (`spread()`). 26 עסקים באורוות האמנים
על נקודה אחת הם סיכה אחת: לוחצים על העליונה ולא מגלים שיש עוד. הטבעת לא מדייקת אותן
ולא מתיימרת — כולן מסומנות "מיקום מקורב" ממילא.

**המקומות מרונדרים כ-circle+symbol ולא כ-HTML markers**, בניגוד לנקודות הציון. 454
markers כבדים ובלי טיפול בהתנגשות תוויות. `markerWaypoints()` מחזיר רק את מה שכן
צריך marker.

**רענון לא הרסני.** הרצה חוזרת מרעננת טקסט ותמונות ולא נוגעת באף מיקום קיים.
`--regeocode` זורק את הנגזרים בלבד; `manual` שורד תמיד.

## כלי סידור המיקומים (נוסף 23/8/2026)

`web/arrange.js`. הרבה מהמיקומים הנגזרים שגויים ואין סקריפט שיתקן, אז זה כלי לעבודה
בכמות. ארבע החלטות שלא כדאי לבטל:

1. **הגרירה נכתבה ביד ולא דרך `draggable: true` של MapLibre.** ה-Marker של MapLibre
   מזיז את הסיכה רק כשמגיע `pointermove`, ולכן מפה שנגללת מתחת לסיכה מוחזקת משאירה
   אותה מאחור. הגלילה מתחת לסיכה היא בדיוק הפיצ'ר שהופך את הכלי לשמיש.
2. **אזור הגלילה נמדד מול השטח הנראה ולא מול הקנבס.** הסרגל מכסה את ראש הקנבס והפאנל
   את תחתיתו, אז אזור בגודל 88px מקצה הקנבס יושב מתחתיהם ולעולם לא נגעים בו. בגרסה
   הראשונה המפה לא נגללה בכלל. בדסקטופ הפאנל הוא עמוד בצד ולא גיליון בתחתית, ולכן
   חיתוך התחתית מותנה בחפיפה אופקית — בלי זה `bottom` יוצא 0 ואין גובה שמיש בכלל.
3. **הגלילה תלוית זמן ולא תלוית פריים.** עם `panBy` לכל פריים, טלפון שמצייר ב-15fps
   נגלל ברבע מהמהירות. יש גם תקרה ל-delta כדי שפריים תקוע לא יזרוק את הסיכה לקצה העיר.
4. **רק הנקודה נתפסת, לא תווית השם.** ה-marker הוא שורת flex של נקודה + שם, ותיבת
   הפגיעה שלו כוללת את השם. הסיכות צפופות, אז השם של אחת מכסה את הנקודה של השכנה,
   ונתפסה סיכה אחרת מזו שכיוונו אליה. `pointer-events: none` על המכל, `auto` על הנקודה,
   ו-`::after` שמרחיב את שטח המגע לאצבע.

**שמירה בצבירה.** `Store.movePlaces` כותב את כל ההזזות ב-commit אחד. אחרת מאתיים
תיקונים = מאתיים הלוך-חזור ומאתיים commits.

## גוגל פלייסס: מוכן, חסום במפתח (23/8/2026)

`build_places.py --google`. **זה המקור היחיד שבאמת יודע איפה עסק קטן נמצא** — נבדק
ונשלל: OSM מכיר 442 נקודות עם שם בכל המושבה, והתאמה מטושטשת מחזירה 6 מועמדים שחצי
מהם שגויים; אין בערכים אף קישור שנושא קואורדינטות (0 מתוך 397 ל-`@lat,lng`, ל-`!3d!4d`
או ל-`maps.app.goo.gl`), ו-Wolt מופיע בערך אחד.

עלות: בקשה שמבקשת `places.location` מחויבת בשכבת Pro, 5,000 חינם לחודש, וכאן צריך
~450 פעם אחת עם מטמון. אפס בפועל. **לפני הפעלת חיוב: מכסה יומית קשיחה בקונסולה, לא רק
התראת תקציב.** בלמים בקוד: מפתח ממשתנה סביבה בלבד, תקרה של 600 קריאות לריצה, סינון
לפי תחום גיאוגרפי, ודרישה למילה משותפת בין שם התוצאה לשם הערך.

## מלכודת: עברית ב-MapLibre הופכה, וגם במפת הבסיס (23/8/2026)

WebGL מסדר גליפים בסדר לוגי ובלי אלגוריתם הכיווניות, אז תווית עברית יוצאת הפוכה:
"דרך הנדיב" מוצג "בידנה ךרד". **גם שמות הרחובות של OpenFreeMap היו הפוכים מאז ומתמיד**
ואף אחד לא שם לב, כי כל תווית שהאפליקציה עצמה ציירה הייתה HTML marker שהדפדפן מסדר.
שכבת המקומות חשפה את זה.

הפתרון: `maplibregl.setRTLTextPlugin`. שתי מלכודות בדרך:
- **גרסה 0.3.0 של התוסף נכשלת** ב-`Failed to import scripts` בלי שום פירוט.
- **טעינה מ-CDN נכשלת** בכל מקרה: התוסף רץ ב-worker שנבנה מ-blob URL, ו-`importScripts`
  חוצה-מקורות לתוך worker כזה לא עובד. לכן `web/vendor/mapbox-gl-rtl-text.min.js`
  (0.2.3, 207KB, ~47KB דחוס) מוגש מקומית. בונוס: עובד גם בלי רשת.

הגליפים עצמם דווקא קיימים: `Noto Sans Regular` טווח `1280-1535` ב-OpenFreeMap. סגנון
הלוויין נבנה ביד ולכן קיבל `glyphs` משלו, אחרת תוויות בו פשוט לא מצוירות.

## מדיה על כל שכבה: `data/media.json` (7/9/2026)

**עד אז אפשר היה לצרף תמונה/סרטון רק לשבילי היוזמה, ולמקומות פרדספדיה סרטון בלבד.**
לא בגלל UI אלא בגלל מבנה: המדיה נשמרה בתוך קובץ השכבה, וכל שכבה אחרת היא קובץ
שסקריפט בונה מחדש, כלומר תמונה ב-`houten.json` הייתה נמחקת בהרצה הבאה.

הפתרון: קובץ לוואי `data/media.json`, ממופה לפי מזהה פריט, עם `photos`/`links`/`note`.
קובץ אחד לכל השכבות ולא קובץ לשכבה, כי מזהה מזהה פריט בלי עזרה, וכך צירוף הוא כתיבה
אחת מול sha אחד.

- **שבילים לא עוברים דרך כאן.** `trails.json` הוא של היוזמה, `restat` סופר בו תמונות
  ו-`export_mymaps.py` קורא אותו. פיצול לשני מסמכים היה שובר שלושה דברים ללא תמורה.
- **המיזוג ב-`reindex()` שב-`layers.js`**, המקום היחיד שכל פריט של כל שכבה עובר בו.
  `mediaBase` היא הרשימה כפי שהמקור מסר, כדי שמיזוג פעמיים לא יוסיף פעמיים וכדי שמסך
  הפריט ידע איזו תמונה מתויקת איפה. תמונות מצטרפות, קישורים מצטרפים בניכוי כפילויות,
  **הערה מחליפה**.
- **המזהה הוא החוזה.** `build_houten.py` ו-`build_network.py` מספרו לפי סדר התשובה של
  Overpass; שביל חדש אחד היה מזיז את כל המספרים ומעביר תמונה לכביש אחר בשקט. היום
  המזהה הוא hash של הגאומטריה (`houten-f93dcbb8`).
- **בשרתון:** `data/media.json` ב-`ALLOWED` וב-`GATED`. בדיקת המבנה שונה: `items` הוא
  מיפוי (מערך נשלל במפורש כי מערך הוא אובייקט), ו**מותר לרוקן אותו** בניגוד לשאר.
- **מלכודת שנתפסה בבדיקה:** `const ownsBase = home === 'trails' || (…)` מחזיר `true`
  בוליאני, והושווה למחרוזת. התוצאה: שביל של היוזמה שכל תמונותיו איבדו כפתור מחיקה.
- סרטון חדש למקום פרדספדיה נכתב לקובץ הלוואי. ישנים עדיין ב-`places.json`, ולכן
  `build_places.py` ממשיך לשמר `yt`.

## קוריטיבה: השכבה השנייה ב"מהעולם" (7/9/2026)

`build_curitiba.py` → `web/data/curitiba.json`. ארבע קבוצות מ-OSM (יחס 297514):
קנאלטות `highway=busway` 91.3 ק"מ, שבילי אופניים 159.7, מדרחובות 12.9, מסלולים
מסומנים 139.9. בנוסף **שש נקודות ציון** (הגן הבוטני, אופרת התיל, בריגווי, טנגואה,
טינגווי, מוזיאון נימאייר), וזו הפעם הראשונה ששכבת `network` נושאת נקודות.

**שלוש מלכודות שהבנייה הראשונה נפלה בהן:**
1. **1,572 ק"מ של "מסלולים"** כי `rel(area.a)` מחזיר כל יחס שנוגע בעיר ו-`out geom`
   מוסר את כולו. תיקון: רק `network=lcn` (לא `rcn` האזורי), וזריקת דרכים שמתחילות
   מחוץ לתיבה התוחמת, דרך שלמה בכל פעם ולא חיתוך קו.
2. **רשת האופניים התרוקנה ל-44 ק"מ** כי `Rede Ciclovias de Curitiba` הוא יחס אחד
   שמכיל את **כל** הרשת (1,926 דרכים), ודרך שבמסלול נגרעת מהרשת. הוא ברשימת החרגה.
3. **148 שורות בשם "שביל"** כי לשבילים אין `name`. תיקון: שיום לפי **שכונה**
   (76 גבולות `admin_level=10`), עם בדיקת הכלה אמיתית ולא "המרכז הקרוב", כי שכונות
   ארוכות וצרות לאורך נחל מחזירות בקביעות את המרכז של השכנה. 305 חסרי שם, 32 נשארו.

**דילול גאומטריה, וזו השכבה היחידה שכך.** `Store.load()` מושך את כל המסמכים בטעינה
בין אם השכבה דלוקה ובין אם לא, אז 1.3MB היה נוחת על כל מבקר. `simplify()` הוא RDP
איטרטיבי בסובלנות 4 מ׳ → 406KB, שהם 55KB דחוסים, בערך כמו האוטן. רץ **אחרי** `chain`
(הקצוות לא זזים) ו**לפני** `line_id` (המזהה הוא hash של הקו שמצויר, כך שנדנוד של מטר
ב-OSM לא מנתק תמונה). שבילי היוזמה לא נוגעים בזה.

**מדיה: תמונה אחת וסרטון אחד לכל נקודה ולשלושה מקטעים, 18 בסך הכל.**
- **תמונות מוויקישיתוף בקישור ישיר.** `upload.wikimedia.org` מגיש
  `access-control-allow-origin: *` ובלי `Cross-Origin-Resource-Policy`, ההפך הגמור
  מ-My Maps. נבדק ברינדור בכרומיום ולא רק ב-curl.
- הקרדיט **נקרא מוויקישיתוף בזמן הבנייה** ולא נכתב בקוד, ונכנס ל-`cap`.
- שתי רזולוציות (500/1600) ולעולם לא המקור: חלקן ברוחב 5000 פיקסל.
- **פרמטרי utm נגזרים** מכתובות ה-API של ויקישיתוף.
- **מזהי יוטיוב נבדקים מול oEmbed בכל בנייה** (חינם, בלי מפתח). 404 חוזר גם לסרטון
  שהוסר וגם לכזה שההטמעה בו כבויה, וזה אותו דבר למפה.
- הכותרות והמזהים **כתובים בקוד ולא מחופשים בזמן הבנייה**, כדי שחיפוש לא יחליף תמונה
  בשקט בחודש הבא.

## מצב תעופה: `web/explore.js` (8/9/2026, פקדים חדשים 11/9/2026)

מסך מלא, תמונת לוויין, והמשתמש מרחף מעליה. דרך קיצור נדלקת רק כשמתקרבים אליה,
והתמונות שלה תלויות באוויר מעל השביל וגדלות ככל שמתקרבים. הכניסה בכפתור החמישי
בטור, `#explore`.

**הפקדים מ-11/9/2026, אחרי שאורי טס עם הגרסה הראשונה:** העכבר הוא ההגה (מיקום הסמן
ביחס למרכז, בלי לחיצה, רק ימינה ושמאלה), הכפתור השמאלי הוא המצערת (לחוץ מגביר,
משוחרר דועך לאט, 2 שניות למלא ו-3.5 בחזרה), W/S עילרוד (W אף למטה), A/D גלגול בלי
הגבלה, חצים גובה, Enter גלריה של השביל הקרוב בתוך הטיסה, M צליל. בטלפון אצבע על
המסך היא המצערת וגרירה פונה.
- **ההגה נתפס בתזוזה הראשונה של העכבר, ונכנס ברכות** (`armed` + `stickGain`, ריבועי
  על 1.4 ש'). עד 16/9/2026 בלילה ההגה נתפס רק כשהסמן עבר דרך הטבעת במרכז, וכרטיס
  הפתיחה חסם אותו 9 שניות; אורי טס ודיווח "בהתחלה לא פונה, אחרי זמן כן, לא ברור מה
  משנה", וזה נראה לו כבאג. שני המנגנונים היו מכוונים (סמן שנשאר בקצה אחרי סגירת חלון
  זרק את המטוס לפנייה) אבל לא הסבירו את עצמם. עכשיו: הזזת העכבר ב-60 פיקסל סוגרת את
  כרטיס ההמראה (רק אותו, `introTimer` מבחין מעזרה שנפתחה ב-?), כל תזוזה תופסת את
  ההגה, וההגנה היא הרמפה. הטבעת עדיין פועמת בזהב כשההגה לא תפוס (עד התזוזה).
- **כשהסמן נח על כרטיס 120 אלפיות ההגה משתחרר** והפנייה גוועת מהר, כדי שהכרטיס
  יפסיק לברוח מהסמן. לכל כרטיס הילת לחיצה של 18 פיקסל (`.fly-card-hit`), כי כרטיס
  נע הוא מטרה שצריך לפגוע בה מכלי טיס. Enter הוא הדרך ללא עכבר.
- **הטיה מייצרת פנייה** (`sin(bank) * BANK_TURN`), ולכן גלגול מלא יוצא לאותו כיוון
  שנכנס, ומטוס הפוך טס ישר. כשמשחררים A/D הכנפיים מתיישרות בדרך הקצרה.

**גלגול מלא: המפה היא ריבוע בגודל האלכסון, ושדה הראייה מורחב כדי לפצות.** מלבן
מסובב מכסה את המסך בכל זווית רק אם הוא ריבוע שצלעו האלכסון. זה היה מצר את המבט,
כי MapLibre קובע את מרחק המצלמה לפי גובה המכל, אז `fitFrame()` מרחיב את ה-fov
(57 מעלות על 1440x900) בדיוק כך שהחלון מראה את אותם 32 המעלות האנכיות שהיו קודם.
אותה מצלמת חור סיכה עם חיישן גדול יותר; `focal` נגזר מה-fov שנקבע בפועל, ו-`zoomFor`
ו-`horizonY` משתמשים בו. ב-MapLibre 4 זה `map.transform.fov` (מוגבל ל-60; ב-5 יש
`setVerticalFieldOfView`). מסך רחב במיוחד יקבל מבט מעט צר יותר במקום פינה שחורה.
בטלפון אין גלגול והמכל בגודל המסך (`--fly-ox/--fly-oy` אפס).

**מלכודת: ההבהובים השחורים אחרי ההגדלה היו הקומפוזיטור, לא האריחים.** אורי דיווח על
"חלקים שחורים, כאילו לא מספיק לצייר". מדידה: 74 אריחים בלבד, זום מרבי 17, והדהייה
של אריחים כבויה בתנועה. מה שנשאר: שכבות מצוירות בגודל 1700x1700 (שמיים, כרטיסים)
שהסתובבו בכל פריים **וגם צוירו מחדש בכל פריים** דרך `height` ו-`left/top`; כשכרום לא
מספיק לרסטר אריח של שכבה כזאת הוא מציג כלום, ומאחור היה שחור. התיקון: כל מה שזז
עובר ב-`transform` בלבד (רצועת שמיים בגובה קבוע שמוזזת, `BAND_H` חייב להתאים ל-CSS),
לכל כרטיס ושבב שכבה משלו, בלי `backdrop-filter` על השבבים, רקע בגוון אדמה (`#23261e`)
מאחורי הכול ומתחת לאריחים, ותקרת `setPixelRatio(1.5)` בטיסה. לא נבדק בעין מכאן,
רק בהיגיון; אם עדיין מהבהב, השאלה היא איפה.

**המנוע מסונתז ב-Web Audio, בלי קבצים** (`Engine` בתוך explore.js): רעש חום דרך
מסנן נמוך שנפתח עם המהירות, רעש לבן בפס 2.6 קילוהרץ לאוויר, מסור וסינוס שלא בדיוק
באוקטבה לשריקה, ומבער שהוא רעש חום דרך `tanh` עם רטט 9.5 הרץ. `AudioContext` נוצר
בתוך הלחיצה על הכפתור (אחרת הדפדפן משתיק), ו-`poke()` מחדש אחרי השהיה. נמדד
ב-OfflineAudioContext שהרמות נכונות; איך זה נשמע, רק אוזן תגיד.

**הגלריה בטיסה** מציגה את כל התמונות והסרטונים של הפריט (סרטון = רשומה ב-`photos`
עם `yt`, מתנגן ב-iframe של youtube-nocookie). סגירה חייבת `src='about:blank'` ואז
הסרת התכונה, כמו בלייטבוקס של האפליקציה, אחרת הפריים טוען את האפליקציה בתוך עצמה.
הטיסה מוקפאת ולא נעצרת: המהירות חוזרת כשסוגרים.

**מלכודת קטנה: `.fly-hint { display: inline-flex }` גובר על `[hidden]` של הדפדפן.**
הרמז "Enter לפתיחה" הוצג גם בלי שביל מתחתיו מאז 8/9 ואף אחד לא שם לב. כל אלמנט עם
כלל display צריך `[hidden] { display: none }` משלו; ב-app.css אין כלל גלובלי כזה.

**נבנה על MapLibre ולא על Three.js.** מה שהיה חסר זה לא מנוע גרפי אלא מודל טיסה:
הלוויין, התבליט והמצלמה שנוטה ל-80 מעלות כבר היו כאן. הקובץ הוא בעצם שני דברים,
מצלמה שחושבת במטרים מעל הקרקע, וכלל חשיפה שמחליט מה כבר גילית.

**המצלמה כנה.** `alt` הוא גובה אמיתי במטרים, והזום נגזר ממנו דרך הגאומטריה של
MapLibre עצמה (`zoomFor`): המצלמה יושבת `0.5 / tan(fov/2) * height` פיקסלים ממרכז
המבט, ולשדה הראייה של 36.87 מעלות זה בדיוק 1.5 גבהים. אותו מספר נותן גם את קו האופק
(`horizonY`). זה מה שמאפשר לתמונות להיות בגודל אמיתי במטרים ולגדול מפרספקטיבה ולא
מנוסחת מרחק מזויפת. הטווח שיוצא הוא זום 13.9 עד 18.2, כולו בתוך מה ש-Esri מספק.

**גודל התמונה על המסך מחושב מהקרנה ולא מנוסחה:** מקרינים את הנקודה ועוד נקודה 40 מ׳
לצדה, והמרחק ביניהן במסך הוא פיקסלים למטר. גובה תלייה מעל הקרקע הוא אותו מספר כפול
`sin(pitch)`, כי וקטור מאונך לקרקע מוקרן לציר המסך האנכי בדיוק בגורם הזה.

**מלכודת: התבליט הורס את התמונה בטיסה.** בהטיה של 70-80 מעלות רשת ה-DEM נראית כמעט
מהצד, ומכיוון שהאריחים נגמרים בזום 14 והטיסה יושבת ב-16 עד 18, כל משולש נמתח על פני
חמש רמות זום. התוצאה היא שהחצי הקרוב של התמונה הופך לפסים אנכיים של צבע. **נמדד
בהשוואה ישירה** ולא הונח. הפתרון: `setTerrain(null)` במצב תעופה, ומחזירים ביציאה.
לא מפסידים כלום, התבליט האמיתי כאן הוא 46 מ׳ על פני כל המושבה.

**מלכודת שנייה, וזו עדינה: `position: absolute` שחורג שמאלה מרחיב את שטח הגלילה,
וב-RTL הראשית נעוצה בימין.** המפה מוגדלת ב-8% מכל צד כדי שהטיה בפנייה תכסה את
הפינות, ואז `scrollWidth` הפך מ-1440 ל-1555 והשמינית השמאלית של המסך נפלה מחוץ למפה
והראתה את הרקע הלבן של הדף. אלמנט `position: fixed` לא תורם לשטח גלילה בכלל, וזה
התיקון. אותה מלכודת מחכה לכל אלמנט שיחרוג מהמסך באפליקציה הזאת.

(עד 11/9/2026 ההטיה הוגבלה ל-6 מעלות והמפה הוגדלה ב-8% בלבד, עם `rollLimit()` שפתר
את הזווית המרבית לפי יחס המסך. הוחלף בריבוע האלכסון, ראה למעלה.)

- **המקשים נקראים מ-`e.code` ולא מ-`e.key`**, כי בפריסה עברית ה-`key` של W הוא 'ט'.
- **סינון התמונות הוא על המיקום המוקרן ולא על הכיוון בלבד.** בדיקת כיוון פוסלת רק מה
  שמאחורי המצלמה; תמונה קרובה וקצת מהצד עוברת אותה ובכל זאת נוחתת אלפיים פיקסל מתחת
  לתחתית המסך, כי בהטיה הזאת הקרקע שמתחתיך לא בתמונה. שמונה מתוך עשרה מקומות התבזבזו
  ככה. הכלל הנכון: להקרין, ואז לפסול מה שלא חותך את החלון הנראה.
- **ה-URL לא מתעדכן בטיסה.** `jumpTo` בכל פריים מייצר `moveend` בכל פריים, ו-Safari
  חוסמת כתיבות היסטוריה בקצב כזה. `syncView` יוצא מוקדם כש-`Explore.isOn()`.
- כל שכבות ה-GL של האפליקציה מוסתרות בכניסה ומוחזרות ביציאה לפי מה שנמצא, כי
  "לא רואים עד שמגלים" הוא כל המנגנון, ומפה עם 62 שבילים דלוקים כבר לא מסתירה כלום.
- ביציאה **המיקום נשאר** והשאר חוזר. המקום שאליו הגעת הוא המציאה.

## כדור פורח: כלי טיס שני באותו מצב תעופה (16/9/2026)

אורי ביקש: ליד אייקון המטוס כפתור קטנטן שמחליף למצב כדור פורח. אותם כפתורים, תחושה
אחרת לגמרי: רעש מבער רק בזמן העלייה (חץ למעלה), שאר הטיסה שקטה לחלוטין, שליטה
פיזיקלית עם התנודות, ומהירות מהירה מהמציאות כדי להגיע ממקום למקום.

**מבנה.** הכול ב-`explore.js`, בלי קובץ חדש: `craft` הוא `'jet'|'balloon'`, נשמר
ב-`localStorage['dk.fly.craft']`, נקרא בהמראה וקבוע לטיסה. **ברירת המחדל מ-16/9/2026
(ערב) היא הכדור**: אורי רוצה שמי שנכנס לראשונה יראה אותו; index.html מתחיל עם
`img/balloon.svg`, `body.craft-balloon` וכותרות תואמות כדי שלא יהבהב מטוס לפני app.js. `step()` מסתעף ל-`jetControls`
(הקוד הישן, הוצא לפונקציה, לא שונה) או ל-`balloonControls`; המצלמה, החשיפה, הכרטיסים
וה-HUD משותפים. `Explore.setCraft/getCraft` ל-app.js; `Explore.debug()` חושף את מודל
הטיסה לבדיקות. הכפתור `#craft` ב-index.html (26px, מראה את הכלי **האחר**, זה שיוחלף
אליו), התמונה על הכפתור הגדול מתחלפת (`img/balloon.svg`, אותו צהוב על אותה עיגולית
ירוקה). מיקום הכפתור הקטן משוכפל בבלוק המסך הרחב, כמו כל הטור (המלכודת מ-9/9).

**הפיזיקה, ממקורות אמיתיים ואז דחוסה.** Daidzic (Aviation 25:3, 2021): כדור AX8 של
3,000 מ"ק, 800 ק"ג, קוטר 18 מ', שטח גרר 254 מ"ר, C_D 0.5 בעלייה ו-0.9 בירידה,
7.5 ק"ג עילוי לכל מעלה מעל 90°C, טיפוס מרבי 3.4 מ'/ש', נפילה סופית של מעטפת קרה
8 מ'/ש', מסה אינרציאלית = מבנה + אוויר חם + מסה נגררת. ספר הטיסה של ה-FAA (פרק 7):
הבערה תקנית 4 שניות, טיסה ישרה = אחת כזאת כל 25–40 שניות, **הכדור מגיב 6–15 שניות
אחרי ההבערה**. מה שנשמר: הצורה של כל זה. חום → פלומה (השהיה `MIX_T` 3 ש') → חום
מעטפת `dT` (קלווין מעל הסביבה), דעיכה אקספוננציאלית `COOL_T` 220 ש', פתח אוורור מוריד
7 K/ש', עילוי = `LIFT_K`×(dT − 80), מסה 3,000, גרר ריבועי 22/40. מה שנדחס פי ~3:
החימום (6 K/ש'), הקירור, ומהירויות אנכיות (סופית ~8 מ'/ש' בהבערה של 3 ש'). נמדד
בכרום: הבערה של 3 ש' מקצה נמוך מעט משיווי משקל, השיא של קצב הטיפוס מגיע 15–20 ש'
**אחרי** שחרור המקש, החום ממשיך לעלות אחרי השחרור (הפלומה), ואז מתקרר ושוקע. מעטפת על
הקרקע לא מתקררת מתחת ל-62 K, אחרת המראה מחדש היא רבע שעה.

**הרוח היא השקר היחיד.** `WIND_BASE + alt×WIND_K` (6 + 0.05/מ', בין 7 ל-28 מ'/ש'):
ב-100 מ' 40 קמ"ש, ב-260 מ' 68, ב-440 מ' 100. במציאות 15. הכפתור הוא "לתפוס את הרוח":
`ride` עולה ל-1 ב-τ=2 ש', יורד ל-0.32 ב-τ=10 ש' ולעולם לא לאפס, כי כדור פורח לא עומד.
העכבר מסובב את הסל (26°/ש' בקצה, כבד), וכיוון הסחיפה `drift` מתיישר אחרי המבט ב-τ=2 ש'.

**התנודות הן הלב.** הסל הוא מטוטלת מתחת למרכז המעטפת: שני מתנדים מרוסנים (גלגול,
עלרוד) עם מחזור 7 ש' ו-ζ=0.12. הכוח: כל תאוצה אופקית של המעטפת (רוח שנתפסת, פנייה)
הופכת לזווית שיווי משקל שהסל נע אליה ומעבר לה, עד 6°; המבער שנדלק נותן דחיפה קטנה;
נגיעה בקרקע חבטה; ושלושה ערוצי "טורבולנציה" (`turb`: שלושה סינוסים בלי מחזור משותף)
דואגים שזה לא ייעצר אף פעם, כולל נדנוד איטי של הכיוון. הגלגול מוחל דרך אותו `bank`
של המטוס (סיבוב CSS); **בטלפון, בניגוד למטוס, יש סיבוב**, ולכן `fitFrame` נותן
למפה שוליים של 8% כדי שהפינות יהיו מכוסות עד 9°.

**הצליל.** `Engine` פוצל לשני אפיקים (`jetBus`, `balBus`) על אותו master ומדחס; הקולות
של כל כלי נבנים בפעם הראשונה שהוא טס. המבער: רעש חום דרך bandpass 240 Hz (הגוף),
רעש לבן ב-950 Hz (הקצה) ומעל 2.6 kHz (הסילונים), הכול דרך gain "פרפור" שמונע ברעש
מסונן ל-11 Hz, ו"וומפ" של הצתה (סינוס 150→36 Hz בשליש שנייה). נמדד דרך AnalyserNode
על המדחס: RMS 0.21 בהבערה, מקדם השתנות 0.24 (מפרפר), מרכז ספקטרלי 374 Hz, שיא 172 Hz,
**ו-0.00000 בכל שאר הזמן**. השקט הוא חצי מהעיצוב. הבערת המראה (`LAUNCH` 2.5 ש' מעבר
להנעת ההמראה) מלמדת מה המקש עושה בלי מילים.

**מראה.** גוף `fly-balloon`: שמיים של שחר (הכדורים טסים בזריחה), זוהר המבער יורד
מלמעלה, בלי פסי מהירות, שפת סל קלועה בתחתית עם שני כבלים בצדדים (קבועים למסך: הסל
הוא מה שעומדים בו, האופק הוא שזז). מדים: גובה, **וריומטר** (▲/▼ מ'/ש', המכשיר של
טייס כדור), מהירות, "מבער דולק"; הפס תחת המדים הוא חום המעטפת, כחול מתחת לסימן
שמחזיק גובה וכתום מעליו. בטלפון המדים יורדים לשורה משלהם (ארבעה לא נכנסים ליד
שלושת הכפתורים). כרטיס פתיחה נפרד לכל כלי.

**מלכודת שנתפסה בדרך: ל-Esri אין זום 19 מעל המושבה.** האריח חוזר 200 עם "Map data not
yet available" אפור (2,521 בייט). המקור היה `maxzoom: 19`, ולכן כל מבט למטה מגובה נמוך
(W בכדור, ואולי גם במטוס) צייר אפור. תוקן ל-18 ב-app.js, והמפה מותחת את האריח האחרון.

**בדיקה:** `tests/test_balloon.py` (בריפו, לא ב-scratchpad שנמחק בין סשנים), Playwright על כרום אמיתי, 60 בדיקות, כולל מדידת
הצליל. **חובה `--use-gl=angle --use-angle=gl`**: עם swiftshader ה-fps הוא 1.3 ומודל
הטיסה רץ ב-8% מזמן אמת (השעון של `dt` נחתך ב-0.06 לפריים), וכל בדיקת זמן משקרת.
עם angle/gl: 54 fps, 0.99 ש' מודל לכל שנייה. גם דף הפתיחה (`dk.welcome.v1`) חוסם
לחיצות ב-Playwright ויש להסתירו.

## המטוס הוא F-16 (16/9/2026, ערב)

אורי ביקש: להגיד למשתמש בתחילת הטיסה שזה F-16, להתאים את הגבלת הגובה והמהירות
לנתוני F-16, ואפקט מעבר מהירות הקול "כמו במציאות". המספרים הציבוריים, בבלוק
הכיוונון בראש `explore.js`: תקרה 15,240 מ' (50,000 רגל), מאך 1.2 קרוב לקרקע ומאך
2.05 ב-12 ק"מ ומעלה (`MACH_SL/MACH_HI`, אינטרפולציה לינארית בגובה), מנוע יבש עד
מאך 0.95/1.1 (`MACH_MIL_*`), דטנט המבער ב-66% מהמצערת (`MIL`; ה-CSS של פס המצערת
צבוע מ-66%), טיפוס 254 מ'/ש' (50,000 רגל/דקה) מעל ~300 מ' (מתחת נשאר האקספוננציאלי
הישן), צלילה 400, ו-9 g.

**מודל המהירות הוחלף.** במקום `speed → throttle×top` עם קבוע זמן 0.45 ש', יש עכשיו
מהירות יעד (ריבועית מתחת לדטנט, כדי שהתחתית תישאר הליכה, לינארית מהדטנט עד הגבול)
ו**תאוצה מוגבלת**: דחיפה 8/15 מ'/ש'² יבש/מבער (אמיתי 6/11, דחוס פי 1.35), בלימה
12 + 10×(v/top)². מהירות הקול ISA (340.3 − 0.00411×h עד 11 ק"מ, אז 295.1), ומאך נמדד
נגד הערך המקומי. **פס טרנסוני** מאך 1 ± 0.05: הדחיפה יורדת עד חצי במרכזו (המבער
"נלחם"), טלטלה של עד 5 פיקסל על המפה/שמיים/עולם (גם בטלפון), והבום כשעוברים 1.0
עם היסטרזיס 0.985. נמדד בכרום: מאך 1 אחרי 24 ש' של כפתור לחוץ מ-260 מ', 1,450 קמ"ש
בשיא, חזרה מתחת למאך 1 אחרי 4 ש' של שחרור, מתחת ל-100 קמ"ש אחרי 26, התקרה אחרי 59 ש'.

**הפנייה מוגבלת ב-g, לא בעכבר:** `yawCap = 9g/v`. במאך 1.2 זה 12°/ש' ורדיוס 2 ק"מ,
במהירות נמוכה הרף גבוה מכל מה שהעכבר מבקש. בלי זה 70°/ש' במאך 1.2 היה 12 g. מד g
ב-HUD (מוסתר בטלפון, כי שם הסיבוב הוא החלקה ולא קצב). מד מאך, כתום מעל 1
(`body.fly-super`).

**מה שלא F-16, בכוונה:** אין רצפת מהירות. F-16 אמיתי מאבד הזדקרות מתחת ל-250 קמ"ש;
כאן שחרור המצערת עוצר באוויר, כי בלי לעצור אי אפשר להסתכל על תמונה. כרטיס הפתיחה
אומר את זה במפורש. **הבעיה המובנית:** המושבה 4 ק"מ, ובמאך 1 חוצים אותה ב-12 ש'.
הבום תמיד יקרה מעל השדות או הים. זה מה שהתבקש; הפתרון למי שרוצה לטוס מהר הוא לעלות,
ב-5 ק"מ רואים את כל מישור החוף. רדיוס החשיפה כבר היה תקוע ב-3 ק"מ מעל 600 מ', אז
התקרה החדשה לא חושפת יותר מהתקרה הישנה.

**הבום.** `Engine.boom()`: שני פולסים במרווח 0.11 ש' (גל N של חרטום וזנב), כל אחד
רעש חום ב-lowpass 110 Hz עם התקפה של 12 אלפיות + סינוס 72→26 Hz. **המנוע מונמך
ל-8% למשך 0.3 ש'** דרך gain `eng` שכל קולות המטוס עוברים בו: בלי זה, במבער מלא,
הבום הוסיף חמישית לרמה וכלום לאוזן (נמדד: RMS 0.32→0.39). עם ההנמכה: שיא 0.53 מול
0.25, מרכז ספקטרלי 222→63 Hz, ואז שקט 0.04 והמנוע חוזר. הטייס האמיתי לא שומע את
הבום שלו. ענן ההלם: `.fly-cone` טבעת לבנה שמתרחבת מהמרכז החוצה ב-1.1 ש' + הלבנה
קצרה, מופעלת בשני הכיוונים (מחיקת class, reflow, החזרה).

**מעבר למאך 1 המנוע משתתק** (אורי שאל, והפיזיקה מסכימה): קול המנוע נע במהירות הקול
דרך אוויר שזורם אחורה מהר ממנה, אז שום דבר מהזרבובית שמאחור לא מגיע קדימה; טייסים
אמיתיים מדווחים על תא שקט, ומה שנשאר הוא הרוח על גוף המטוס (מקומית) ושארית דרך
המתכת. מימוש: gain `hush` אחרי `eng` (0.07 מעל מאך 1, τ 0.5 ש', חוזר ב-0.35), וההיס
של האוויר מחובר ישירות ל-`jetBus`, לא דרך `eng` ולא דרך `hush`. נמדד: RMS יורד
מ-0.31 ל-~0.1 ומרכז הספקטרום עולה לאלפי הרץ; חוזר כשיורדים מתחת למאך 1.

`tests/test_f16.py`: אותו ריג של הכדור, ~3 דקות, 70 בדיקות. המעבר נקרא בלולאה מהירה
(`PROBE`, ~800 קריאות ב-1.4 ש') כי דגימה כל רבע שנייה מפספסת שני פולסים ו"דאק"
של שליש שנייה. קצב הטיפוס נבדק כממוצע על כל הטיפוס ולא לדגימה: ג'יטר של סיבוב
ה-round trip ו-`dt` שנחתך ב-0.06 נותנים 200–270 בין דגימות בודדות.

## מצפן ב-HUD (16/9/2026, ערב)

אורי ביקש מצפן בשני כלי הטיס, לאוריינטציה. `.fly-compass` במרכז העליון: חוגה קבועה
(למעלה = לאן מסתכלים, שנתות כל 10°, ארוכות ברבעים) ו**מחוג** ב-SVG (`#fly-needle`)
שמסתובב `rotate(-bearing)` דרך attribute (זול, סביב ראשית ה-viewBox שהיא המרכז),
**חצי אדום לצפון** וחצי לבן לדרום, פס צהוב קבוע בראש הטבעת, ומתחת הכיוון במילים +
מעלות ("צפון־מערב 315°", 8 רוחות, מתעדכן רק כשהמספר המעוגל משתנה). הגרסה הראשונה
הייתה שושנה מסתובבת עם אותיות צ/מז/ד/מע; אורי ביקש מחוג אדום ובלי האותיות. בכדור
גם **חץ כחול על השפה לכיוון הסחיפה** (`bal.drift` יחסית למבט), כי שם לאן פונים
ולאן נסחפים הן שתי שאלות. בטלפון: שורה 1 כפתורים + מצפן, שורה 2 מדים (86px),
פס מצערת 132px, עם `body.flying` בסלקטור כי הכללים הפשוטים בהמשך הקובץ גברו על
media query קודם באותה ספציפיות (זה היה באג שקט: המדים ישבו על המצפן).

## שלוש תצוגות בטיסה: רגיל, נקי, שבילים (17/9/2026)

אורי ביקש כפתור עם שלושה מצבים: כמו היום; בלי תמונות ובלי שמות השבילים (התמונות
מסתירות את השטח); ורק שמות השבילים עם השבילים מוארים הרבה יותר חזק. וכשאין שכבת
שבילים דלוקה, רק שני המצבים הראשונים.

**מימוש ב-`explore.js`:** `view` הוא `'normal'|'clean'|'trails'`, נשמר
ב-`localStorage['dk.fly.view']` כמו כלי הטיס והצליל. `buildWorld` קובע `hasTrails`
(יש `line` בעולם), ו-`viewsNow()` מחזיר שתיים או שלוש אפשרויות; `setView` על
`'trails'` בטיסה בלי שבילים נופל ל-`'clean'` (וכך גם בהמראה עם ערך שמור). ההצמדה
לשכבה היא לפי "יש קו בעולם" ולא לפי שכבת דרכי הקיצור בשם, כך ש"טיולים" לבדה גם
נותנת שלושה מצבים. `applyView()` מסתיר כרטיסים/שבבים, מחליף paint לשתי שכבות
הזוהר (`trailPaint(view)`: ב-trails הזוהר `#ffb340` ברוחב 16→64 במקום 9→36,
הליבה לבנה 4→13 במקום 2.4→8), מאפס `tick` כדי שה-reveal ירוץ בפריים הבא, ומעדכן
את הכפתור. ב-`recompute`, ב-trails, `g` מוגבר ל-`1-(1-g)³` (חמישית מהדרך פנימה כבר
חצי מואר, בלי קפיצה בקצה) ו-`h = g` במקום `0.7g`. `paintCards` יוצא מוקדם כשלא
normal, `paintChips` כש-clean; ב-trails עד 12 שמות (4 בטלפון) במקום 5 (2).

**הכפתור `#fly-look`:** רביעי בשורת הכפתורים (`inset-inline-end: 152px`, כלומר
**שמאל** ב-RTL, ליד X/?/צליל; אורי הציע "למעלה מימין" אבל שם יושבים המדים). אייקון
לכל מצב (תמונה / תמונה מחוקה / שביל זוהר, קבוצות SVG שמוחלפות ב-`data-view`), המילה
מתחת לעיגול (`<b>` ב-`position:absolute; top:100%`, כמו הכיוון מתחת למצפן), ובמצב
שבילים העיגול זוהר בזהב. `title` אומר מה המצב ומה הלחיצה הבאה נותנת. מקש **V**
מדפדף. בטלפון הכפתור יורד לשורה משלו (`top: 58px`) כי רביעי בשורה נוחת על המצפן.
`body[data-fly-view]` ל-CSS (שבבים זוהרים יותר ב-trails).

בדרך תוקן דבר קטן: שבב שם שמרכזו מעבר לקצה החלון (המפה חורגת מהמסך ב-`ox`) היה
מילה חתוכה בקצה; עכשיו מוסתר, אותה לוגיקה כמו בכרטיסים (`rolled` = כל המכל).

**בדיקה:** `tests/test_view.py` (43 בדיקות, כדקה): שלושת המצבים במקש ובכפתור, paint,
`g` מוגבר, שמירה בין טיסות, שני מצבים בלי שבילים, ומיקום בטלפון. `Explore.debug()`
חושף `view/views/hasTrails/cards/chips/lit`.

## שכבת אירועי דרך הנדיב: `build_hanadiv.py` (17/9/2026)

אורי ביקש שכבה "אירועי דרך הנדיב" מ-2026.hanadiv.org, ממוקמת כמה שיותר מדויק.
**האתר הוא Angular מעל API פתוח** ב-`api.hanadiv.org` (נמצא ב-`main.js`:
`Mt_apiURL`): `/festival?festival=2026` (תת-הדומיין הוא המפתח), `/event`, `/place`.
46 אירועים ב-21 כתובות, 29–31/10/2026. ל-`/place` יש lat/lng לכל מקום, ולכן אין צורך
בגיאוקודינג מאפס; הסקריפט בודק ומתקן. התמונות ב-`api.hanadiv.org/images/` עם CORS
פתוח ובלי CORP, ולכן מקושרות ישירות (בניגוד ל-My Maps).

**מקורות אימות שעבדו:** OSM Overpass עם `addr:housenumber` (דליל: ברקאי כולו, ארבל,
הדקלים 95, גינת אגוז, יונה); **B144** (`b144.co.il/maps/פרדס-חנה-כרכור/<רחוב>/<מספר>/`
מדפיס lat/lng ב-HTML, מספור מונוטוני לאורך הרחוב, אינטרפולציה); **gushim.co.il**
(Next.js, צריך Playwright; 404 לכתובות שאינו מכיר) → גוש/חלקה → מרכז מ-govmap WFS;
**עמוד ענן** (amudanan.co.il) לאתרים בשם. לא עבדו: govmap geocode (דורש טוקן),
Nominatim (רמת רחוב), Madlan (חוסם headless). **שבעה תיקונים ב-`FIXES`** ושני מקרים
שבהם B144 טעה וה-API צדק (מתנ"ס, אמפי): גיאוקודר מנחש מספר בית לאורך הרחוב, סימון
אתר בשמו מנצח. פרטים בדוקסטרינג של הסקריפט.

**באפליקציה:** `HANADIV_ID='hanadiv'`, `addPlaceLayer` כמו אמנות במושבה, כבויה
כברירת מחדל, `?layers=hanadiv`. סיכה לכל אירוע (id `hanadiv-<eventID>`), `spread`
מקומי שמפזר כל ערימה בלי קשר למקור. שדות חדשים שהאפליקציה מציגה: `when` (צ'יפ כהה
`.chip.when`, גם בשורת הרשימה ובחיפוש) ו-`audience`; `craft` = הז'אנר. מקורות
מיקום חדשים ב-`GEO_SOURCE`: `b144`, `landmark`. הקובץ הועלה לריפו הנתונים
(`data/hanadiv.json`, בוטי) ומוגש מ-raw.

## תצוגה מקדימה בוואטסאפ: `web/og.jpg` ו-`build_og.py` (11/9/2026)

אורי שלח את הקישור לקבוצה וקיבל שם דומיין בלבד. לא הייתה אף תגית Open Graph. עכשיו
יש `og:title`, `og:description` בקול של האפליקציה (יחיד, בלי מספרים כדי שלא יתיישן),
וכרטיס 1200x630 מהג'קרנדה הפורחת (`72b4d97bc41f5e`) עם מספר השבילים החי.

- **וואטסאפ לא מפענח webp בתצוגה מקדימה**, וכל 168 התמונות באפליקציה הן webp. הוא
  מוריד, נכשל, ומציג בדיוק מה שהיה מוצג בלי תמונה, כלומר התקלה בלתי נראית. לכן
  הכרטיס הוא JPEG שנוצר ב-Chromium (עברית + פונט Heebo מגוגל, Pillow לא יודע בידי).
- **התגיות כפולות: גם ב-`web/index.html` וגם ב-`index.html` של ההפניה.** סורק שמקבל
  את הכתובת הקצרה קורא את דף ההפניה ועוצר, הוא לא עוקב אחרי `meta refresh`.
- **וואטסאפ מטמין תצוגה לפי כתובת.** קישור שכבר נשלח לא יתעדכן; `?v=2` פעם אחת
  נותן תצוגה טרייה.
- `og:image` חייב כתובת מוחלטת. `set_content` ב-Playwright פותח דף על `about:blank`
  ומשם `file://` חסום בשקט, לכן התמונה מוטמעת כ-data URI.

## קישורים ותמונות על שביל שכבר פורסם (23/8/2026)

- `links: [{url, title}]` על מקטע, נקודת ציון, טיוטה ומקום. נרנדר לכולם, לא רק לעורך.
- `cleanLinks` ב-store.js מסנן: רק http(s), משלים סכימה חסרה, זורק `javascript:`
  ו-`data:`. הקובץ משותף לכמה כותבים, אז URL כזה הוא סקריפט שרץ אצל כולם.
- מחיקת תמונה מסירה רק את ההפניה ולא את הקובץ מהריפו. שמות הקבצים הם hash של תוכן,
  אז אותה תמונה יכולה להיות משויכת לשני שבילים, ומחיקה הייתה מרוקנת אחד מהם. יתום
  עולה כמה מאות KB, חסרה עולה תמונה.

## מלכודות ב-MapLibre שכבר נפלנו בהן
- **ביטוי `["zoom"]` חייב להיות החיצוני ביותר.** `['case', sel, 9, ['interpolate',
  ['zoom']...]]` נדחה עם "zoom expression may only be used as input to a top-level
  step or interpolate". הפתרון: interpolate בחוץ, ו-`case` בכל עצירה.
- `maplibregl.supported()` **הוסר בגרסה 3**. שימוש בו מחזיר "אין WebGL" בכל דפדפן
  והמפה לא נבנית בכלל. הבדיקה היא ישירות על קנבס (`webglAvailable`).
- `setStyle` מוחק כל source ו-layer שהוספנו. לכן `applyOverlays` רץ מחדש בכל
  `style.load`, אחרת החלפת רקע מוחקת את השבילים ואת התבליט.
- **`setStyle` חייב `{diff: false}` (נתפס 8/9/2026).** בלי זה MapLibre מנסה קודם
  *לתקן* את הסגנון הרץ לסגנון החדש, ותיקון כזה יורה `styledata` ולעולם לא
  `style.load`. התוצאה: השבילים, התבליט וקו הניווט נמחקים בדיף ואף אחד לא מחזיר
  אותם. האם MapLibre בכלל תנסה לתקן תלוי בכמה שני הסגנונות רחוקים, ולכן החלפת רקע
  רגילה עבדה חודשים בעוד יציאה ממצב תעופה, שנגעה בשכבות בדרך פנימה, השאירה מפה
  ריקה. **בלי הודעת שגיאה.** טעינה מלאה תמיד עולה כמה אריחים ונותנת התנהגות אחת.
- פקדי המפה יושבים גבוה ב-z-index. סרגל הניווט חייב 1100, אחרת הוא לא רק מצויר
  מתחתיהם אלא גם לא מקבל לחיצות.

## צל עצים: השכבה הראשונה מסוג `raster` (13/9/2026)

אורי ביקש לבדוק את goinfo.co.il (אתר נתונים אזרחי עצמאי, 74 כלים מעל מקורות
ממשלתיים) ולראות מה נוח לקחת ממנו לשכבות. הממצאים שנשארים נכונים:
- **ל-GoInfo אין API ואין הטמעה של שכבות, ואין CORS.** הנתונים המחושבים בקבצי JS
  בתוך האתר (`src/*.js`), התמונות ב-`assets/blobs/`. כל שימוש = העתקה בזמן בנייה.
  הנתונים שחישב הם CC BY 4.0 (ציון + קישור). הגולמיים של הממשלה.
- רוב האתר לא שייך למפת שבילים, ותכניות/ייעודים/גושים כבר יש לנו מאותם מקורות.
  מה שכן שווה: **חופות עצים** (מפ"י 2021, נבנה), **תאונות הולכי רגל** (למ"ס דרך
  data.gov.il, `SUG_TEUNA=1`, X/Y ברשת ישראל; ב-2024 13 מ-33 תאונות במושבה, לא נבנה),
  **רישיונות כריתה פתוחים להשגה** (`trees.goinfo.co.il/statics/reports/open_for_objection.html`,
  שורות `<tr data-record>` עם קישור govmap שנושא קואורדינטות, 17 למושבה, משתנה
  יומית; יש RSS לישוב ב-`storage.googleapis.com/yeela-504306-feeds/פרדס_חנה-כרכור/`),
  ו**הפקעות לפי תכנית** (`hafkaot-map.html?plan=<מספר>`, נוסף כקישור על כל תכנית).
- הוחלט (אורי): אופציה 2, רסטר מהתמונה של GoInfo. אופציה 3 (לבנות מקובץ מפ"י
  358MB + מדד צל לכל שביל) נשמרה להמשך אם השכבה תופסת.

`build_canopy.py` → `web/data/canopy.json` + `web/overlays/canopy/` (27 אריחים,
350KB, בריפו הקוד ולא בריפו הנתונים). פרטים מלאים ב-README, "צל עצים".

**מלכודת: מקור `image` של MapLibre (תמונה + 4 פינות) נקטע עם התבליט.** עם
`setTerrain` דלוק, בזום 16.5 החופות נעצרו בקו אופקי ישר באמצע המושבה; בלי תבליט
חזרו. זה MapLibre 4.7 שמצייר לתוך אריחי התבליט ולא מוצא את התמונה לחלק מהם. הפתרון:
אריחי XYZ (512px, זום 10–14) שנחתכים בסקריפט, עם `bounds` על המקור. בדיקה: צילום
עם/בלי השכבה בכמה זומים ופיץ' והשוואת פיקסלים. `new URL(template)` מקודד את `{z}`
ל-`%7Bz%7D`; לבנות `new URL('.', location.href).href + template`.

`kind: 'raster'`: בלי פריטים, `drawnIds` = `ras-<id>`, מצויר מתחת לשכבת ה-symbol
הראשונה של סגנון הבסיס (`underLabels`), דירוג 0.15, ב-`shareable`. הרשימה מציגה
"מה שדלוק הוא תמונה על המפה" כשרק היא דלוקה. השדה `tileBounds` ולא `bounds`, כי
`bounds` על שכבה גורם ל"טוס לשם" (האוטן/קוריטיבה).

בדיקה: `scratchpad/test_canopy.py` (Playwright על כרום אמיתי, 16 בדיקות). ה-404 של
`img/*_t.webp` במצב `?local` הם עותק לא מקוון ישן ולא באג.

קישור בתוך תיאור שכבה: שדה `noteLinks` (שם → כתובת) על השכבה, ו-`noteHtml` ב-layers.js
עושה escape לכל הטקסט ורק אז מלביש `<a target="_blank">` על השמות. תיאורים של שאר
השכבות נשארים טקסט בלבד (תושבים כותבים אותם). קישור בתוך ה-label לא מטגל את הצ'קבוקס.

## ניווט: למה הוא פנימי
83% מהשבילים (41 מתוך 49) לא קיימים ב-OpenStreetMap, ולא ברשת של גוגל. לכן שום
מנוע ניתוב לא יכול להוביל דרכם, והפניה לגוגל מנווטת **סביב** קיצור הדרך. הניווט
עובד על הגיאומטריה שלנו: עד הכניסה, ואז ספירה לאחור לאורך השביל.

**היציאה ננעלת** במגע הראשון עם השביל. חישוב מחדש בכל עדכון GPS הפך את החץ באמצע
השביל והחזיר את המשתמש מאיפה שבא.

## תקלה בנתוני המקור
לאחד המקטעים קוראים `jחצר אחורית הצפירה - בית כנסת -תמר`, עם j מיותר בהתחלה.
זו טעות הקלדה במפה עצמה, לא באפליקציה. תיקון צריך להיעשות ב-My Maps.

## מצב המפה
| תאריך | מקטעים | נקודות ציון | אורך כולל |
|---|---|---|---|
| 8/2026 (כתיבת הערך) | 35 | 7 | ~3.4 ק"מ |
| 15/8/2026 | 49 | 11 | 4,828 מ' |

בנוסף, מ-22/8/2026, שכבות רשת הרכיבה מוסיפות 79 מקטעים ו-54 ק"מ (כבויות כברירת מחדל).

המפה גדלה בקצב מהיר, ולכן כל מספר שנכנס לערך הוויקי מתיישן. שווה לחשוב על עדכון אוטומטי.

## נושאים פתוחים (עברו מ-pardespedia)
1. קישור המפה בערך הוויקי הוא לינק **עריכה** (`/edit`) ולא צפייה בלבד. אורי טרם החליט אם להחליף.
2. גרסה ישנה של קובץ "דרך קיצור - מפת השבילים המתהווה.jpg" בוויקי מכילה תמונת פנים של אורי
   בפינה. הגרסה הנוכחית חתוכה ותקינה, אבל ההיסטוריה עדיין מכילה אותה. מחיקה דורשת מנהל.
3. היוזמה הייתה "מתחת לרדאר": שמות חברי הצוות (מלבד יואב), פרטי דיון המועצה והמסמך הפנימי
   הוסרו מהערך בכוונה. לוודא לפני כל פרסום נוסף שהמצב הזה עדיין תקף.

## הפרדה מ-pardespedia
הקוד ישב עד 15/8/2026 בתוך `pardespedia/`. הוא הועבר לכאן כי היוזמה עומדת בפני עצמה
והוויקי הוא רק אחד הצרכנים שלה. אין יותר הפניות אליו מתוך pardespedia.
