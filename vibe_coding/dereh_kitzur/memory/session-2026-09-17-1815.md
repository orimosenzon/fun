# סשן 2026-09-17

## מה נעשה

**כפתור תצוגה בטיסה: רגיל / נקי / שבילים** (קומיט `563db20`). אורי ביקש כפתור
עם שלושה מצבים: כמו היום; בלי תמונות ובלי שמות השבילים, כדי לראות את השטח;
ורק שמות השבילים עם השבילים מוארים הרבה יותר חזק. `view` ב-`explore.js`, נשמר
ב-`localStorage['dk.fly.view']`, מקש V, כפתור `#fly-look` רביעי בשורת הכפתורים
(שמאל ב-RTL; אורי הציע "ימין" אבל שם המדים, וזה לא הפריע לו). אייקון לכל מצב,
המילה מתחת לעיגול, זוהר זהוב במצב שבילים. בלי שכבת שבילים דלוקה שני מצבים בלבד,
ו"שבילים" שמור הופך ל"נקי". במצב שבילים: `g` מוגבר ל-`1-(1-g)³`, זוהר ענבר רחב
כמעט פי שניים, ליבה לבנה, עד 12 שמות. בדרך: שם שביל שמרכזו מחוץ לחלון כבר לא
מופיע חתוך. `tests/test_view.py` (43 בדיקות), ושתי החבילות הישנות עדיין עוברות
(128). אורי: "בדיוק מה שרציתי".

**הסבר בעשר נקודות** על מקורות המפה (OpenFreeMap/Liberty, Esri World Imagery עד
זום 18, Terrarium) והשרתון (Cloudflare Worker שמחזיק את מפתח גיטהאב היחיד, שתי
דרגות, ההגנות). לא שינה קוד; התוכן כבר ב-project-knowledge ו-worker/README.

**שכבת אירועי דרך הנדיב 2026** (קומיט `776697c`). האתר `2026.hanadiv.org` הוא
Angular מעל API פתוח ב-`api.hanadiv.org` (`/festival`, `/event`, `/place`, עם
`festival=2026`). `build_hanadiv.py` מייצר `data/hanadiv.json`: 46 אירועים ב-21
כתובות, 29–31/10/2026, סיכה לכל אירוע (טבעת סביב מקום עם כמה אירועים), 14
ז'אנרים ב-7 קבוצות. **המיקומים נבדקו מול מקור שני** (OSM `addr:housenumber`,
B144, gushim.co.il→govmap WFS, עמוד ענן) ושבעה תוקנו ב-`FIXES`, כולל 0,0,
כתובת במרכז המושבה 2.4 ק"מ מהרחוב, ומספר בקצה הלא נכון של הצפירה. שני מקרים
שבהם B144 טעה וה-API צדק (מתנ"ס, אמפי) מתועדים כדי שלא "יתוקנו". באפליקציה:
`HANADIV_ID`, צ'יפ `when` כהה (גם ברשימה ובחיפוש), `audience`, מקורות מיקום
`b144`/`landmark`. הקובץ הועלה לריפו הנתונים (בוטי) ומוגש מ-raw. פרטים
ב-project-knowledge.md ובדוקסטרינג של הסקריפט.

## מצב נוכחי

- הכל חי ב-Pages אחרי `776697c`; `data/hanadiv.json` בריפו הנתונים.
- השכבה כבויה כברירת מחדל; `?layers=hanadiv` פותח אותה. נבדקה בכרום מול השרת
  המקומי עם `?local` (חלונית, טבעת, תמונות, סרטוני יוטיוב), לא מול הכתובת
  הציבורית אחרי הדחיפה.
- השרת המקומי על 8080 כובה.
- עדיין פתוח מסשנים קודמים: הכדור לא נטוס על ידי אורי מאז 16/9; קבועי `BAL`,
  `ARM_T`, `A_MIL/A_AB`, `BRAKE_0`; צל העצים לא נראה בעין.

## צעדים הבאים

1. לפתוח את האתר החי עם `?layers=hanadiv` ולוודא שהשכבה נטענת מ-raw (לא רק
   מהעותק המצורף), ולבדוק בטלפון את הטבעת של המתנ"ס והצ'יפים.
2. לשקול להדליק את השכבה כברירת מחדל בשבועיים שלפני הפסטיבל (29/10), ולשלוח
   קישור לקבוצה. אחרי הפסטיבל לכבות או להוריד.
3. להריץ `build_hanadiv.py --report` שוב קרוב לפסטיבל: אירועים נוספים ושינויים
   ב-API; FIXES נשאר.
4. כיוונון אחרי טיסה: מצב "שבילים" (`trailPaint`, `MAX_CHIPS_TRAILS`), והישנים:
   כדור (`BAL`), רמפת ההגה (`ARM_T`), זמן עד מאך 1.
5. פתוחים ישנים: צל עצים בעין, תאונות הולכי רגל, `?v=2` לקבוצה, הוספה מהטלפון.
