# סשן 2026-04-08

## מה נעשה
- סשן ראשון על הפרויקט
- גילינו שהקוד הבסיסי (HTML/JS/world.js) כבר קיים אבל אף פעם לא עבד end-to-end
- הגדרנו חזון: "Lazy World Generation" — נרטיב ותמונות נוצרים on-demand ונשמרים
- מימשנו:
  1. `generateLocationImage()` ב-api.js — Imagen 3, cache ב-localStorage
  2. `loadLocationImage()` ב-game.js — non-blocking, מעדכן UI כשמוכן
  3. מפה SVG דינמית ב-ui.js — נבנית ב-BFS, מתעדכנת עם כל מעבר

## בעיות שנתקלנו
- `gemini-2.0-flash` לא זמין ב-free tier (429) → עברנו ל-`gemini-2.0-flash-lite`
- `gemini-1.5-flash` לא קיים ב-v1beta (404)
- Imagen 3 יתכן שדורש billing — לא אומת עד סוף הסשן

## next steps
1. לאמת שגמיני עובד (נרטיב) עם gemini-2.0-flash-lite
2. לאמת שImagen עובד — אם לא, לשקול חלופה (Stability AI / placeholder)
3. לבדוק שהמפה מוצגת נכון ומתעדכנת כשזזים בין מקומות
4. אם הכל עובד — לשחק ולראות אם הנרטיב איכותי מספיק
