# סשן 2026-06-14 11:38 — בדיקת קווטה Azure gpt-image-2

## מה נעשה

ניסיון לבדוק אם הקווטה ל-gpt-image-2 ב-Azure (East US 2) אושרה.

### תהליך:
1. חיפשנו ב-Azure Portal — לא מצאנו את סקשן ה-Quotas ישירות
2. נכנסנו ל-Microsoft Foundry portal (ai.azure.com) — Quotas נמצא תחת **Operate → Quotas**
3. ניסינו להשתמש ב-Azure CLI (`az cognitiveservices usage list`) לבדיקה פרוגרמטית
4. התקנו az CLI על המחשב הנוכחי
5. ניסינו `az login --use-device-code` מהסביבה שלי — הצגנו קוד `C9EMB89AW` אך הסשן הסתיים לפני אישור

## מה לא הושלם

- לא בדקנו בפועל אם הקווטה ל-gpt-image-2 אושרה
- לא נכתב קוד חדש

## פעולות פתוחות

1. **לבדוק קווטה** — ב-Foundry portal: Operate → Quotas → סנן East US 2 → gpt-image-2
   - או עם CLI: `az cognitiveservices usage list --location eastus2 --query "[?contains(name.value, 'gpt-image')]" -o table`
2. אם אושרה: לפרוס deployment ב-Foundry ולכתוב קוד בפיפין
3. אם לא: לשקול Gemini Flash Image 2.5 (אותו מפתח שכבר יש לשחקן)
