# סשן 2026-04-12

## מה נעשה
- הפעלת Flask dev server (server.py) במקום python http.server — תומך ב-endpoint `/log`
- הוספת logger.js — שולח שגיאות לשרת ב-POST /log, נשמר לקובץ pipin_errors.log
- חיבור לוגר ל-api.js: כל 429, כישלון מודל, ותגובה ריקה נרשמים
- תיקון 404 על תמונת hobbiton: הוסרה `src` סטטית מ-index.html
- הסרת שדה `portrait` מכל ה-NPCs ב-world.js (הקבצים לא קיימים בדיסק)
- מעבר מ-Pollinations → Imagen 3 לתמונות לוקיישן ו-NPC portraits
  - callImagen() מחזיר base64 data URL
  - cache in-memory (Map) במקום localStorage — base64 גדול מדי
  - generateNPCPortrait() מיוצאת ומשמשת ב-ui.js (אסינכרוני)
- תיקון quota Gemini: הוגדר budget ב-Google Cloud (התראות בלבד, לא עצירה)
- ניסיון להגביל הוצאות — Google לא מאפשרת עצירה אוטומטית, רק התראות

## ממצאים
- Gemini quota נגמר (429 על שני המודלים) — ייתכן שמתחדש בלילה
- Imagen 3 גם לא עובד כל עוד ה-quota נגמר (אותו מפתח)
- הלוגר עובד ומציג שגיאות בזמן אמת בטרמינל + קובץ

## next steps
1. לבדוק שהנרטיב והתמונות עובדים אחרי חידוש quota
2. Navigation בין לוקיישנים (הפיצ'ר המרכזי החסר)
3. לבדוק layout מובייל
4. לשקול modal מפה למובייל
