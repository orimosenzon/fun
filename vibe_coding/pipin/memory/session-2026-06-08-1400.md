# סשן 2026-06-08 14:00 — הקמת gpt-image-2 ב-Azure לצורך יצירת תמונות מקומות

## מה נעשה

ניסיון להוסיף יצירת תמונות לפיפין באמצעות Azure (בגלל קרדיטים קיימים ב-Founders Hub).

### תהליך:
1. חיפשנו DALL-E 3 ב-Azure AI Foundry — לא זמין בפרויקט.
2. ניסינו MAI-Image-2.5 — **Unsupported region (East US 2)**.
3. ניסינו gpt-image-2 — **Insufficient quota**.
4. הגשנו בקשה להגדלת קווטה ל-gpt-image-2 ב-East US 2, Global Standard.

### פרטי הבקשה:
- **מודל**: gpt-image-2
- **אזור**: East US 2
- **סוג**: Global Standard
- **קווטה מבוקשת**: 50 RPM
- **זמן עיבוד צפוי**: 1-2 ימי עסקים

## מה לא יושם (חוב)

1. **כתיבת קוד בפיפין** לשימוש ב-gpt-image-2 — ממתין לאישור הקווטה.
   - הקוד יצטרך: endpoint של Azure Foundry, API key, deployment name, קריאת images.generate.
   - ה-endpoint הנוכחי: `https://haskala-foundry-resource...` (מ-Home Page של Foundry).
   - הדפוס דומה ל-haskala: `OpenAI(base_url=..., api_key=...)` + קריאת `images.generate(model=..., prompt=...)`.

## פעולות פתוחות

1. **לבדוק** אם הקווטה אושרה (אחרי 1-2 ימי עסקים).
2. **לפרוס** את deployment של gpt-image-2 ב-Foundry.
3. **לכתוב** את הקוד בפיפין לייצור תמונות מקומות.
4. **להוסיף** ל-.env של פיפין: `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_ENDPOINT`, `AZURE_IMAGE_DEPLOYMENT=gpt-image-2`.
