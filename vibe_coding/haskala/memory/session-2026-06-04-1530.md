# סשן 2026-06-04 15:30 — מדיניות שמות Word סופית + הכנת פריסה

## מה נעשה

### מדיניות שמות קבצי Word (סופי)
- **ללא קו-תחתון בשם הקובץ:** `basename_OCR_model.docx` / `basename_Eval_model.docx`
- **עם קו-תחתון בשם:** `basename_OCR_model_YYYYMMDD.docx` / `basename_Eval_model_YYYYMMDD.docx`

דוגמאות:
- `moshe.pdf` → `moshe_OCR_claude-opus-4-8.docx`
- `moshe_cohen.pdf` → `moshe_cohen_OCR_claude-opus-4-8_20260604.docx`

### קוד שנשתנה
- `app.py` — נוסף `MODEL_FILENAME_LABEL` dict + `_docx_filename(base, kind, provider)` helper
- `app.py` — שני ה-endpoints מקבלים `model` מה-body ומשתמשים ב-helper
- `app.py` — `_inject_models()` מחזיר גם `model_filename_labels`
- `templates/index.html` — `window.__haskalaMfls` מוזרק מה-template
- `templates/index.html` — `docxName(kind)` פונקציה חדשה; שני handlers משתמשים בה ושולחים `model` ב-POST

### Git commit
`5b1b46f` — haskala: new Word filename policy (basename_OCR/Eval_model[_date])

## הכנת פריסה (לא הושלמה)
- SSH ל-HF לא עבד — המפתח הישן (`haskala_key`, fingerprint `SHA256:Ybe8oL6/xZTReK6ay1pfJDCeQQM/1tSaklJGmLXEwmk`) לא נמצא במכונה.
- נוצר מפתח חדש: `~/.ssh/haskala_key` (fingerprint `SHA256:brjyc7rCtKABw7Zv7sdOMJRbWzi5ToUrBhonf6h834c`)
- נוצר `~/.ssh/config` עם הגדרות GitHub + HF
- **פעולה נדרשת מאורי:** ב-[huggingface.co/settings/keys](https://huggingface.co/settings/keys) — למחוק את `haskala_key` הישן ולהוסיף:
  ```
  ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOKFVe8m8Ka4vEQXAcJrZCGU1z8QSmC5rp9RmTW7tlvr haskala-hf
  ```
- אחרי עדכון HF — להריץ `./deploy.sh` ולשכפל את ה-Space (יתבקש clone בפעם הראשונה)

## חובות פתוחים
1. **פריסה** — אחרי עדכון מפתח SSH ב-HF
2. בדיקה על ערבית ב-Space (כלל 9 + פידבק בערבית)
3. אימות Opus 4.8 — אם 404, לחזור ל-4.7
4. בנצ'מרק Claude/Gemini/Groq על כתב יד עברי
5. אזהרת אבטחה: GROQ_API_KEY ששותף בצ'אט — לבדוק/לבטל
