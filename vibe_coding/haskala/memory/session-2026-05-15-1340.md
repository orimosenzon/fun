# סשן 2026-05-15 13:40 — תיעוד הפעלה ב-README

## מה נעשה
- עדכון [README.md](../README.md) של haskala: הוספת סעיף "תוכניות בפרויקט" עם הוראות הפעלה מלאות ל-OCR POC.
- מבנה: סקירה קצרה → דרישות מוקדמות → התקנה (venv + pip + .env) → הפעלה → שימוש → פרמטרים שכדאי להכיר (RENDER_DPI, padding, בחירת מודל) → מקום שמור לתוכניות עתידיות.
- סטטוס בקובץ עודכן מ-"שלב תכנון" ל-"שלב POC".
- קוד הליבה לא נגע.

## החלטות עיצוב ל-README
- מבנה היררכי שמאפשר להוסיף תוכניות נוספות בקלות תחת "תוכניות בפרויקט" (סעיף 2, 3, ...).
- שמירה על RTL עם `<div dir="rtl">` סביב חלקי עברית, יציאה מ-RTL ל-code blocks ולחזרה אחריהם.
- לינקים לשורות ספציפיות ב-app.py עבור הפרמטרים החשובים (#L125, #L87).

## מצב הקוד
זהה לסשן הקודם — `app.py` POC עובד על port 5050, ממשק templates/index.html, alignment שורה-שורה עם padding 30px ו-normalize_line_heights. לא נבדק על דוגמאות חדשות בסשן הזה.

## צעדים הבאים (לא השתנו מהסשן הקודם)
1. **לבדוק את ה-POC על דוגמאות אמיתיות יותר** — PDFs שונים (יישור משתנה, איכות צילום נמוכה, ערבוב שפות) ולהעריך אם איכות ה-OCR מספיקה.
2. **להכריע בשאלה הקריטית** — נקודת כניסה של התלמיד: Classroom רגיל או companion app? משפיעה על כל הארכיטקטורה.
3. אם איכות OCR מספקת → לעבור לבדיקת תשובות, וזה מעורר את שאלת מפתח התשובות (טקסט / צילום / AI / היברידי).
4. אם איכות OCR לא מספקת → דיון: Sonnet vs Opus, או image processing מקומי לחיתוך שורות במקום להסתמך על קואורדינטות מ-Claude.
