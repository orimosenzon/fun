# Session 2026-05-13 21:20 — פתיחת פרויקט haskala

## מטרת הסשן
פתיחת פרויקט חדש לאדמיניסטרציה אוטומטית של כיתת לימוד, מבוסס Google Classroom + Claude Vision.

## מה נעשה
1. **שלב סקופינג ראשוני** — תיאום עם המשתמש על תפקיד המערכת, תחום לימוד, גישת OCR, וסטאק.
2. **הקמת תיקיית פרויקט** ב-`/home/ori/fun/vibe_coding/haskala/` עם README ו-memory/project-knowledge.md.
3. **שלד יישומי נוצר ואז הוסר** — לאחר שהמשתמש הבהיר שהוא רוצה לתכנן ולא לממש, הקבצים `app.py`, `requirements.txt`, `.gitignore`, ו-`templates/` נמחקו. נשארו רק README ו-memory.
4. **תיעוד היסטוריה** של גרסה קודמת (2024) ב-project-knowledge.md.
5. **התחלת תכנון user journey** — סקיצה ראשונה של זרימת תלמיד ומורה. הוצגו שלושה פיצולים מרכזיים.
6. **רישום הפרויקט ב-MEMORY.md** של auto-memory.

## החלטות שהתקבלו
- **שם הפרויקט:** `haskala`.
- **סטאק מועדף:** Flask + Claude Vision API + Google Classroom API (OAuth 2.0). *לא* Google Apps Script ולא Classroom Add-on — בגלל שהסטאק תואם לפרויקטים קיימים (letras, nudge, sidekick) ובגלל גמישות מלאה עם Anthropic SDK.
- **תפקיד המערכת:** היברידי — משוב אוטומטי למקרים ודאיים, סקירת מורה למקרים מורכבים.
- **תחום לימוד:** כללי / מגוון (לא חסום למקצוע ספציפי).
- **גישת OCR:** Claude Vision API ישירות (לא Google Cloud Vision, לא Tesseract).
- **שפה:** עברית.
- **שלב פיתוח נוכחי:** *תכנון בלבד*. אין יישום, אין POC, אין קוד פעיל.

## מצב נוכחי של הקבצים
```
haskala/
├── README.md                      ← high-level בלבד
└── memory/
    ├── project-knowledge.md       ← חזון, היסטוריה, סטאק, פיצולים פתוחים
    └── session-2026-05-13-2120.md ← הקובץ הזה
```

## פיצולים פתוחים שעדיין לא הוכרעו

### א. נקודת כניסה של התלמיד
- *Classroom app רגיל* — אפס חיכוך, אבל משוב לא מיידי.
- *אפליקציית companion שלנו* — משוב תוך שניות, אבל עוד אפליקציה להתקין.

### ב. איך המורה מספק "מפתח תשובות" למערכת
- טקסט פתרון בתיאור המטלה.
- צילום דף פתרון של המורה.
- AI פותר מהשאלה ובודק מולו.
- היברידי: AI מציע, מורה תמיד סוקרת לפני שליחה.

### ג. עומק האינטגרציה עם Classroom
- *רק כ-inbox* — Classroom מקבל תמונות, כל השאר אצלנו.
- *אינטגרציה מלאה* — ציונים, comments, ו-rubrics נכתבים חזרה ל-Classroom.

## שלבים הבאים (לסשן הבא)
1. **להכריע בשלושת הפיצולים** — להמשיך את שיחת ה-user journey מהמקום שעצרנו.
2. **לפרט את ה-journey לעומק** אחרי שהפיצולים סגורים — לרבות edge cases (כתב יד לא קריא, ריבוי עמודים, retake, מטלות פתוחות vs סגורות).
3. **לתכנן מודל נתונים** — מה יושב במסד נתונים מקומי vs מה נקרא live מ-Classroom.
4. **לתכנן את ה-API surface** — אילו endpoints ב-Flask, אילו רכיבי frontend.
5. *רק אז*: לעבור ל-POC ראשון (Claude Vision על תמונת שיעורי בית בודדת, ללא Classroom).

## רקע צוותי
הפרויקט מקורי משנת 2024, צוות של שלושה (כולל המשתמש). נעצר אחרי כחודשיים. חזרה ב-2026 בעקבות שיפור משמעותי במודלי vision לכתב יד עברי וכלי קוד אגנטיים.
