# ⚠️ math-checker פרוס עם באג הסיבוב (5/8/2026)

לא סיכום סשן — פתק מצב, כי הבאג תוקן בקוד המשותף אבל **לא נפרס**.

## מה קרה

`ORIENT_MODEL` ב-`haskala_grading/math_core.py` היה Haiku. נמדד מול חמשת עמודי
המבחן לדוגמה: טעה ב-5 מתוך 5. כל העמודים ישרים מלכתחילה, ולכן כל תשובה שנתן
סובבה עמוד קריא לעמוד לא קריא, ומודל הניתוח קיבל עמוד על הצד.

זה מסביר הערות "לא קריא", ציונים נמוכים, ושונות גדולה בין הרצות של אותו קובץ.

תוקן ל-`claude-sonnet-5` עם `thinking` מנוטרל ואימות עצמי של התשובה.
פרטים מלאים: `products/math-form-checker/memory/session-2026-08-05-2105.md`.

## מה עוד צריך לעשות

ה-Space `orimosenzon/haskala-math` לא נפרס מחדש בסשן שבו התיקון נכתב. **כל בדיקה
שרצה שם עד הפריסה הבאה מנתחת עמודים מסובבים.**

```
cd products/math-checker && ./deploy.sh "orientation fix"
```

אחרי הפריסה אפשר למחוק את הקובץ הזה.

## אזהרה לעתיד

אם מישהו שוקל להחזיר מודל זול לשלב הסיבוב — למדוד מול עמודים אמיתיים קודם.
"בלי סיבוב" הוא המקרה השכיח, ולכן מזהה שמנחש גרוע ממזהה שלא עושה כלום.
