# math-form-checker — סשן 2026-08-05

**המוצר נוצר בסשן הזה.** ידע מלא: `project-knowledge.md` שלצידו.

## מה הוחלט

ארבע החלטות שאורי קיבל לפני שהתחלתי, וכל השאר נגזר מהן:

| שאלה | ההחלטה |
|------|---------|
| פורמט הדוח | **Word `.docx`** — לא מומר ל-Google Doc |
| איפה הקוד יושב | **`lib/haskala_grading/`** — מקור אמת יחיד, לא עותק |
| מודל | **Sonnet 5** כברירת מחדל במקום Opus 4.8 |
| ניקוד | **הצעה מסומנת**, בלי מסך אישור |

## מה נבנה

- `lib/haskala_grading/math_core.py` + `math_report.py` — המנוע עבר לשם
- `products/math-form-checker/` — התשתית של form-checker עם המנוע הזה
- `FORM_SETUP.md` — מפרט הטופס (הטופס עצמו טרם נבנה)

130 טסטים עוברים (37 חדשים, 45 math-checker, 48 form-checker ללא שינוי).
הרצת end-to-end מול קבצים אמיתיים עובדת.

## מה נפרס

- **קומיט `c47e90a`** על master, נדחף
- **`math-checker` נפרס ל-HF** (`4298bd4..1845dca`) והגיע ל-`RUNNING`.
  זו הפריסה הראשונה מאז הרפקטור של יולי שכוללת את `core.py`/`report.py`
- **`math-form-checker` לא נפרס** — `deploy.sh` עצר נכון על `RESPONSES_SHEET_ID`

## הממצא שדורש החלטה

אותו דף גאומטריה, אותו מחוון:

| | Sonnet 5 | Opus 4.8 |
|---|---|---|
| זמן | 48ש' | **29ש'** |
| ניקוד | **4/10** | **7/10** |

**Opus יצא מהיר יותר, לא איטי** — טיעון העלות ל-Sonnet 5 עומד, טיעון הזמן לא.
פער של 3 נקודות על תרגיל של 10 גדול מספיק כדי שההחלטה תחזור לשולחן.

## צעדים הבאים, לפי עדיפות

1. **להריץ את שני המודלים על כל `/home/ori/avish/Amir *` ולהשוות.** אם Sonnet 5
   מנקד נמוך באופן שיטתי — לחזור להחלטת המודל לפני פיילוט.
2. **לבנות את הטופס** לפי `FORM_SETUP.md`, ולקחת ממנו את `RESPONSES_SHEET_ID`.
3. **`MATH_FORM_CHECKER_ALLOWED_DOMAINS`** — חובה לפני שהקישור מופץ. הפרויקט
   כבר שילם ~$20 ו-~$26 על שני אירועים כאלה.
4. `RESPONSES_SHEET_ID=… ./deploy.sh` ואז פעם אחת `./setup_infra.sh`.
5. לבדוק מול תיקיית Drive אמיתית (עד כה רק קבצים מקומיים).
6. אופציונלי: להדליק `thinking` ב-`math_core._analyze_anthropic` (כרגע כבוי
   בכוונה) יחד עם העלאת `max_tokens` — מנוף איכות אמיתי, דורש A/B.

## שני באגים קיימים שתוקנו ב-math-checker

- `deploy.sh` ו-Dockerfile לא שלחו את `core.py`/`report.py` מאז 2026-07-03 —
  כל פריסה הייתה מעלה `app.py` בלי ה-imports שלו
- המפתח `sonnet` היה דו-משמעי מרגע ש-Sonnet 5 נכנס לטבלה, והוא היה מפתח מדויק.
  שונה ל-`sonnet46`
