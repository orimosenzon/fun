# Extra deps for the native desktop wrapper (desktop.py) only.
# Not installed in the cloud/web image.
#   pip install -r requirements.txt -r requirements-desktop.txt
#
# On Linux pywebview needs a Qt backend + the qtpy shim; on Windows/macOS
# it uses the OS webview and the Qt lines below are unnecessary.
pywebview>=5.0
qtpy>=2.4 ; sys_platform == "linux"
PyQt5>=5.15 ; sys_platform == "linux"
PyQtWebEngine>=5.15 ; sys_platform == "linux"
