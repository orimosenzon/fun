# סשן 2026-07-20 (16:00) — עצירת שריפת scan2, בניית oris-scanner מאפס, אימות מקצה לקצה

## הקשר
המשך ישיר לסשן 2026-07-17 (ניתוח תיאורטי, ללא לוגים, של מנגנון ה-retry). הפעם:
gcloud אומת בפועל (`ori@bdika.net`, לא `orimosenzon@gmail.com`), ונשלפו לוגים אמיתיים.

## גילוי: השריפה לא נעצרה מעולם
תיאוריית ה-retry אושרה, אבל בהיקף הרבה יותר גדול ממה שהוערך:
- **584,651 בקשות בסך הכל** ל-scan2 בין 2026-07-14 00:00 ל-2026-07-20 12:45 (Cloud
  Monitoring, לא רק דגימת לוגים) — רק 23 הצליחו (2xx), כל השאר 5xx.
- **רץ ברצף במשך שישה ימים**, כולל הרגע שבו נבדק (בקשה אחרונה: 2 דקות לפני העצירה).
- שורש הבעיה: `ackDeadlineSeconds: 10` על ה-subscriptions (`cr-service-europe-west1-
  scan2-gzefnwaz` מ-topic `scanNow`, ו-`-gjvnkal4` מ-topic `checkNow`), בלי
  `retryPolicy` (redelivery מיידי), בלי `maxDeliveryAttempts`, בלי dead-letter —
  ו-`run_workspace_scan()` הוא לולאה סינכרונית שלוקחת דקות. כל delivery מובטח
  להיכשל ולהישלח מחדש, ללא הגבלה.

## פעולה מיידית: עצירה + hardening ל-scan2
1. נותק ה-push endpoint משני ה-subscriptions (`modify-push-config --push-endpoint=""`)
   — הפיך, שום דבר לא נמחק. אושר: אין בקשות חדשות אחרי הניתוק.
2. Hardening (next-step #6 מהסשן הקודם, בוצע בפועל): topic חדש `scan2-dead-letter`
   + subscription, `ackDeadlineSeconds=600`, `retryPolicy` קבוע 600s, `maxDeliveryAttempts=5`
   על שני ה-subscriptions הקיימים. **push endpoint נשאר מנותק** — לא הופעל מחדש.

## נבנה מאפס: oris-scanner
מיקום: `haskala/products/oris-scanner/` (מונוריפו `fun`), אותו פרויקט GCP
(`master-gecko-500709-t0`), region `europe-west1`.

**שחזור קוד scan2 (לא היה בגיט מעולם — פער מתועד מ-2026-07-14/17):** נמשך מה-
container החי דרך Cloud Shell (`docker pull` מ-Artifact Registry + `docker cp
/workspace`), הועבר לגיט. `main.py`, `core.py`, `checker.py`, `report.py`,
`requirements.txt` — עכשיו קיימים ב-`products/scan2/` בגיט.

**ארכיטקטורת oris-scanner (מבוססת על אותו pipeline, שני שינויים מהותיים):**
1. **תוכן:** בדיקת-שפה (transcribed_text + רשימת שגיאות עם type/quote/correction/
   comment), לא מתמטיקה. אושר מול הקוד האמיתי ב-Classroom: המטלה היחידה הרלוונטית
   ("English Homework", courseWork id `868721516278`) היא "Write a short essay
   about your summer vacation", `maxPoints: 100`, בלי רובריקה מובנית.
   - **תגלית לשקול לעתיד:** ב-`/home/ori/avish/` יש טיוטת רובריקה הרבה יותר
     מפורטת (9th-grade ESL, CEFR-aligned, 16 "helpful words" לכל prompt, מבנה
     דוח ספציפי עם הדגשת טקסט/ספירת מילים/צביעת שגיאות) — **לא מחובר עדיין
     לקורס האמיתי**, כנראה טיוטה של ירון לעתיד. לא נבנה נגדה (over-engineering
     למשהו שעוד לא קיים בנתונים), אבל `main.py` כבר שולף `description`+`maxPoints`
     מה-courseWork בפועל ומעביר כ-rubric חופשי — כשירון יצרף רובריקה אמיתית
     לתיאור המטלה, זה ייכנס אוטומטית בלי שינוי קוד.
2. **מודל:** Gemini 2.5 Flash כברירת מחדל (גם לניתוח וגם לזיהוי אוריינטציה) —
   בכוונה, כדי לא להיות תלוי ב-`ANTHROPIC_API_KEY` בכלל (זו הייתה נקודת הכשל
   שגרמה לשריפה המקורית של $20-26 ב-2026-07-14: כשנגמר קרדיט Anthropic, כל
   קריאה נכשלה → 500 → redelivery). מפתח `gemini-api-key` נוצר ב-Secret Manager
   מהמפתח הקיים של `products/checker` (free-tier, אותו אחד).
3. **התיקון הארכיטקטוני המרכזי — פנקס dedup ב-Firestore** (Native, `eur3`,
   נוצר הסשן הזה): לפני עיבוד הגשה, claim אטומי (transaction) ב-collection
   `oris_scanner_submissions`. "done" → מדלגים מיד, אפס עלות. "in_progress"
   טרי → מדלגים (instance אחר). "in_progress" ישן (20+ דקות — claim נטוש) →
   נתפס מחדש ל-retry אמיתי. **זה מה שמונע חזרה על תרחיש scan2** גם אם
   ה-trigger יופעל שוב פעמים רבות.
4. **סינון courseWork:** קורס "integration" הוא קורס-בדיקה משותף (גם מטלת
   אנגלית וגם גאומטריה). `TARGET_COURSEWORK_IDS = {"868721516278"}` מוודא
   ש-oris-scanner לא נוגע במטלת הגאומטריה בטעות.
5. **תיקיית תוצאות:** עודכן לפי בקשת אורי מ-הארד-קוד המשותף עם scan2/מתמטיקה
   לתיקייה ייעודית (`1zzlOq6_UKZJUz33LvzRDqu5F9H-NCmE3`).

## פריסה ובעיות שנתקלו בהן
- **`Failed to find attribute 'app' in 'main'`** — buildpack של functions-framework
  דרך `gcloud run deploy --source .` צריך `--function=process_my_drive_files`
  במפורש (ב-CLI; scan2 המקורי נפרס דרך Cloud Console שמטפל בזה אוטומטית —
  לכן `deploy.sh` של scan2 "עבד" בלי הדגל, אבל רק כי מעולם לא רץ בפועל דרך ה-CLI).
  תוקן ב-`oris-scanner/deploy.sh`. **`scan2/deploy.sh` עדיין חסר את התיקון** —
  לא הרצתי אותו הסשן הזה כדי לא לסכן את הגרסה החיה (המנותקת כרגע) של scan2.

## אימות מקצה לקצה (בוצע בפועל, לא רק תיאורטי)
- Domain-wide delegation אומת: `311716089389-compute@developer.gserviceaccount.com`
  (runtime SA) יש לו `roles/iam.serviceAccountTokenCreator` **ברמת הפרויקט**
  (לא על ה-SA הספציפי) — זה מה שמאפשר את ה-impersonation ל-`sainter@...`
  (subject=`yaron@bdika.net`) גם ב-scan2 וגם ב-oris-scanner.
- הרצה ידנית ראשונה (POST מאומת עם Pub/Sub-shaped body): 3 קבצים מצורפים
  להגשה אחת ("dan sarig") עובדו דרך Gemini (6 קריאות, כולן 200 OK), מוזגו,
  הועלו כ-docx יחיד. הגשה נוספת בלי צירוף תקין דולגה בלי קריסה. 200 OK, ~2.5 דקות.
- **הרצה שנייה זהה מיד אחרי — 2 שניות, אפס קריאות ל-Gemini.** dedup אושר
  אמפירית, לא רק בקוד.
- אחרי שינוי תיקיית התוצאות: פנקס ה-Firestore אופס ידנית (מחיקת 2 מסמכי
  הטסט) כדי לאלץ עיבוד מחדש, ואומת שהדוח נחת בתיקייה החדשה.

## תיעוד
לבקשת אורי: כל ה-# comments ו-docstrings בקוד תורגמו לאנגלית (`oris-scanner/
{core,main,report}.py`, `scan2/{core,report}.py`). **לא נגעתי** ב-ANALYSIS_PROMPT,
בהודעות שגיאה, בתוויות דוח, ב-VERDICT_HE — כל מה שהמורה/מודל רואה בפועל
נשאר בעברית במכוון (אושר מפורשות מול אורי).

## תכנון לעתיד — לא מומש הסשן הזה
אורי שאל על אינטגרציה טבעית יותר ל-Classroom במקום Drive folder הארד-קודד.
המלצה שסוכמה (לביצוע עתידי, לא הסשן הזה):
- לצרף את הדוח **ישירות להגשה הספציפית** של התלמיד דרך
  `studentSubmissions().modifyAttachments()` — מופיע ממש באזור שהמורה בודק
  בו את ההגשה, בלי לנווט ל-Drive בכלל.
- אפשר לשלב גם `draftGrade` על ההגשה (ציון טיוטה שהמורה מאשר/מתקן) — תואם
  את מה ש-`ANALYSIS_PROMPT` כבר אומר ("הניקוד שאתה מציע הוא הצעה לבדיקת המורה").
- ה-scope הקיים (`classroom.coursework.students`) כנראה מספיק, ללא consent חדש.

## קבצים לא-קשורים בגיט status (לא טופלו, לא קשורים לסשן זה)
`haskala/tmp_checker.py`, `haskala/tmp_main.py` — עדיין untracked, כמו בסשנים קודמים.

## צעדים הבאים (לפי עדיפות)
1. **חיבור טריגר Pub/Sub אוטומטי ל-oris-scanner** — `setup_infra.sh` כתוב
   ומוכן (topic חדש `orisScanNow`, hardening מובנה מהתחלה: ack deadline 600s,
   backoff 600s, dead-letter, max 5 attempts) אבל **טרם הורץ**. כרגע oris-scanner
   מגיב רק להפעלה ידנית.
2. **בדיקת איכות הדוח בפועל** — אומת רק ש-pipeline רץ בלי קריסה, לא נבדק
   האם ניתוח השגיאות של Gemini באמת איכותי על הכתב-יד של "dan sarig".
3. אינטגרציית Classroom-native (attachment + draftGrade) — ראה "תכנון לעתיד" למעלה.
4. `scan2/deploy.sh` עדיין חסר `--function=process_my_drive_files` — אם ירצו
   אי-פעם לפרוס מחדש את scan2 עצמו (למשל כדי לתקן את הבאג שם גם, לא רק ב-
   oris-scanner), צריך להוסיף את הדגל קודם.
5. לבדוק חיוב Anthropic בפועל מהתקרית (לא נבדק — Console בלבד, אין admin key מקומי).
6. להחליט על גורל `products/scan2/` בטווח הארוך — oris-scanner מיועד להחליף
   אותו (כיוון נכון + תיקון dedup), אבל scan2 עדיין קיים ורשום בגיט כעת.
