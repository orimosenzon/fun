# oris-scanner — session 2026-07-30 09:26

Short session, but it closed the single most important open item: **Claude Sonnet 5
graded a real submission in production for the first time.** Also: gcloud installed
locally at last, and the poll rate moved back to every minute.

## 1. gcloud CLI installed locally

It was never actually installed on the Pardes Hanna machine (open item, long-standing).
Installed the official tarball into `$HOME/google-cloud-sdk` (v578.0.0) — no sudo, no
snap confinement quirks that could interfere with OAuth. PATH added to `~/.bashrc`
(backup at `~/.bashrc.backup`). Auth was already live as `ori@bdika.net`, project
`master-gecko-500709-t0`. Note the login-refresh gotcha still stands: use
`gcloud auth login --force`, plain login silently reuses stale creds.

## 2. Poll rate `*/5` → `*/1` (every minute)

At Ori's request — responsiveness over the ~$2/month CPU overage. Done in both places
this time (the 07-30 drift lesson): live Cloud Scheduler job **and** `setup_infra.sh`
(SCHEDULE default, inline comment, echo banner, and the cost/gap comment block all made
coherent). Verified live: 12 consecutive 1-minute polls, all HTTP 200, ~7s each
(container reaped between polls, so each pays a cold-ish start).

## 3. FIRST real Sonnet 5 production grading — SUCCESS (open item #1, CLOSED)

Avishai turned in at 04:54:30 UTC on "מטלה יפיפיה ליום חמישי בבוקר" (course
`integration - Ori`). Full trace, end to end, no errors:

- Detected 04:55:06 — **36s after turn-in**, thanks to `*/1`.
- Submission: `In class writing practice First draft…pdf`, 703KB, 1 page.
- Rubric: `RubricsCD2020.pdf` (Module C/D) attached to the assignment, matched **by
  filename** ("Rubrics") → old name-match path.
- 3 Anthropic calls, all 200: rotation-detect → OCR (11 lines) → eval.
- `[eval] overall=3/30`, `[check] done in 40.4s`, 44 words. Report docx uploaded to
  "תרגילים בדוקים". `scan finished in 53.2s — 1 submission graded`.
- subm id `Cg4IwfXMnKQZELvVwfCtGQ`.

**What this proves:** Sonnet 5 works in prod, including the two `max_tokens=10` calls
that were the migration's real risk (no empty strings, no crash). No OOM at 2Gi.

**What it still does NOT prove:** the content-based rubric classifier — this rubric
matched by name, so it went down the old path. Still unexercised in production.

## 4. Rubric comparison (side task)

Ori dropped two official Ministry writing rubrics into a new `haskala/rubrics/` folder
and asked for a comparison:
- `FGExternalInternal2020.pdf` — Module G/F (5 יח״ל), 40 pts.
- `RubricsCD2020.pdf` — Module C/D (4 יח״ל), 30 pts.
Key difference: G/F puts 40% of the weight on Language Use (grammar); C/D puts the most
weight (33%) on Content. Higher level also demands Band-3 vocab, ≥3 advanced structures,
paragraphing, longer word counts. Produced a Hebrew RTL PDF summary:
`haskala/rubrics/rubrics-comparison.{html,pdf}` (rendered with the same headless-Chrome
line the docs use). Note: `oris-scanner/rubrics/ministry-writing-module-c-d.json` looks
like an encoded RubricsCD2020; there is **no** JSON yet for Module G/F.

## Production state at session end

- `oris-scanner-00029-kkm`, 2Gi / 1 vCPU, all secrets intact (unchanged this session).
- Scheduler `* * * * *`, ENABLED.
- `DEFAULT_MODEL = claude` (Sonnet 5) — now proven live.
- Avishai's submission graded 3/30, report in "תרגילים בדוקים".

## Open items (priority order)

1. **Eyeball the 3/30 report** for grading quality — a 44-word first draft against a
   70–90-word Module C/D rubric. Length deduction alone is ~6; 3/30 may be right or may
   be too harsh. This is the first live sample of *evaluation quality* — the real test.
   Reading it needs Drive access on local gcloud (unverified).
2. **Measured Sonnet 5 cost.** The call happened but logs carry no token counts; still
   the ~$0.043 estimate. Pull from console.anthropic.com → Usage, or add a usage log line.
3. **Confirm the Anthropic spend limit (~$10)** — raised twice, never confirmed.
4. **Content-based rubric classifier still unproven in prod** (this run matched by name).
   Same for the `[model: …]` tag (none was used) and the native Classroom rubric path.
5. **Retry around Classroom calls** — one 503 still kills a whole scan. A few lines of
   try/except, offered repeatedly, never approved.
6. **Memory headroom unmeasured** — 2Gi cleared 903MiB on a single page; true peak unknown.
7. `company-knowledge.md` still documents only `checker` — no oris-scanner / math-checker
   / scan2. Carried since 07-25.
8. Lasting gcloud auth fix: a service account with `roles/logging.viewer` +
   `roles/run.developer` via `GOOGLE_APPLICATION_CREDENTIALS`, plus
   `roles/iam.serviceAccountTokenCreator` on `sainter@…` for local Classroom debugging.
9. `issue quote not found` warnings (pre-existing); scan2's fate; build a Module G/F
   rubric JSON.
