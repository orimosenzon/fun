# סשן פרדספדיה — 2026-07-26 22:17

## מה נעשה

### 1. שאלת מודלים במנגנונים האוטומטיים (בירור, לא שינוי)
מיפוי מה מריץ מה בארבעת ה-cronים:

| מנגנון | מודל | תקציב |
|---|---|---|
| `cron_update_events.sh` → `update_events.py` | **אין LLM כלל** — Python דטרמיניסטי | — |
| `cron_all_bot_talk.sh` | `--model sonnet` | $2.00 |
| `cron_contributor_comments.sh` | `--model sonnet` | $3.00 |
| `cron_enrich.sh` | `--model sonnet` | $2.00 |

המודל **hardcoded** בדגל `--model sonnet` בכל סקריפט — אין לוגיקת בחירה.
לוח האירועים בכלל לא עובר דרך מודל: `update_events.py` מושך את הפיד מ-eventschedule.com
וממיר ל-wikitext ישירות.

**נקודה פתוחה להחלטה:** האם להעלות את `cron_all_bot_talk.sh` לאופוס. זה המנגנון היחיד
מבין השלושה שכותב טקסט חופשי בעברית לדף ציבורי בלי בקרה אנושית מראש — טעות ניסוח שם
נראית לכל המושבה. השניים האחרים פחות רגישים (מחמאות תבניתיות; דוח העשרה הוא קובץ מקומי
שנקרא לפני שנוגעים בוויקי). השינוי = מילה אחת בשורה אחת.

### 2. שתי תמונות חדשות מ-~/Pictures/פרדספדיה/ — הועלו וטופלו

**א. מגדל המים בכרכור** (`מגדל המים בכרכור.jpg`)
- הועלה עם `upload_local.py`, רישיון CC BY-SA 4.0, צלם: אורי מוסנזון
- **לא נפתח ערך חדש** — הערך `מגדל המים` כבר קיים כ-stub על מגדלי המים במושבה
  (עם `{{הרחבה}}`). פתיחת ערך נפרד הייתה מפצלת stub דל ממילא.
- במקום זה: נוסף סעיף `== המגדל בכרכור ==` עם התצלום, לפני `{{הרחבה}}`.

**ב. כיכר יין-יאנג** (`כיכר יין-יאנג.jpg`)
- כדור מתכת חלול שהחליד, דופנו נחתכה בצורת סמל יין-יאנג תלת-ממדי.
  במפגש דרך הבנים / רחוב המושב, כרכור.
- **נפתח ערך חדש: `כיכר יין-יאנג`** — לא היה שום כיסוי (חיפוש על
  "פסל"/"אמנות"/"יין ויאנג" החזיר אפס תוצאות בוויקי).
- סעיפים: הפסל · סביבת הכיכר · אמנות ציבורית בדרך הבנים · ראו גם · קישורים חיצוניים.
  קטגוריות: תרבות מקומית, תחבורה. (אין קטגוריית "אמנות" בוויקי — שווה לשקול ליצור.)
- קישור דו-כיווני: נוסף סעיף `==כיכרות ואמנות ציבורית==` ל-`דרך הבנים (רחוב)`.
- האינדקס עודכן (`update_index.py --write`) — נוספו 4 שורות, כולל הערך החדש.

## החלטות תוכן שכדאי לזכור

- **לא יוחס יוצר לפסל.** אורי שיער בשם הקובץ "כנראה גבריאל והאנס בנו", אבל חיפוש רשת
  לא אימת זאת. בערך נכתב במפורש: "זהות היוצר ומועד ההצבה טרם תועדו בפרדספדיה" —
  עדיף חור מתועד מאשר ייחוס שגוי בערך על אמנות ציבורית.
- **הפסל אינו אנדרטת השילוב.** צורת היין-יאנג (שני חצאים משתלבים) מפתה לקשור אותה
  לאיחוד פרדס חנה-כרכור 1969, אבל אנדרטת האיחוד עומדת ב`גן יד לשילוב` בצומת
  דרך הבנים / הרב יוסף קארו — צומת אחר. שני פסלים נפרדים.
- הקשר שאומת ונכנס לערך: ב-2006, לקראת סיום סלילת דרך הבנים מחדש, הזמינה המועצה
  מהפסל תושב כרכור בנימין ויטל עשרה פסלי עזים לכיכרות המושבה (מקור: ויקיפדיה העברית).

## מצב סוף סשן

- אין שרתים או תהליכים לוקליים פתוחים (לא הופעל אף אחד בסשן הזה).
- ארבעת ה-cronים פעילים כרגיל.
- 4 עריכות בוויקי: `מגדל המים` (rev 5343), `כיכר יין-יאנג` (rev 5344, חדש),
  `דרך הבנים (רחוב)` (rev 5345), `האינדקס של הדפים` (rev 5346).

## צעדים הבאים (לפי עדיפות)

1. **חוב העשרה — עדיין הפער הגדול ביותר.** `enrich_reports/enrich-2026-07-25-0630.md`
   מצא 49 ערכים חדשים, טופלו 8. **41 ערכים מ-22-23/7 לא נגעו בהם.** המחקר כבר עשוי
   בדוח (טקסט מוצע + מועמדי תמונות + וידאו). הסקיל `pardespedia-enrich` מבצע בפועל.
2. **להחליט על מודל ה-bot-talk** (סעיף 1 למעלה).
3. **לשאול את הקהילה מי יצר את פסל היין-יאנג** — פוסט בכיכר חנה. לא נשלח בסשן הזה
   (הודעה חיצונית → דורשת אישור).
4. חמישה ערכים ללא קישור למושבה: בית עלמין כרכור-כפר פינס-עין עירון, כיכר מקלף -
   דרך הים, פארק זהר, אילנה אביב, המאפיה.
5. תמונה חופשית מוכנה לקטיף לבית העלמין פרדס חנה: `File:Pardes_Hanna_cemetery2018.JPG`
   בוויקישיתוף (`Category:Pardes Hanna cemetery`).
6. **`rebuild_mainpage.py` עדיין untracked** — לקמש או למחוק.
7. סתירה לבדיקה בפארק הבמבוק: המקור החיצוני אומר "קבוצות סגורות בתיאום מראש בלבד",
   הערך משדר אווירה קהילתית פתוחה.
