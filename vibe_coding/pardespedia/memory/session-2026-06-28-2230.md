# סשן 2026-06-28 ערב (פרדספדיה)

## מה נעשה

### ארכוב כיכר חנה
לפי בקשת המשתמש: כל התוכן הקיים של [[כיכר חנה]] (שיחות 2019–2026) הועבר ל[[כיכר חנה/ארכיון 1]] (snapshot מלא + תיבת הערת-ארכיון). הדף הראשי נוקה והושאר נקי: באנר ברכה + תמונת חנה + **תיבת לינק בולטת (אייקון 🗄️, מסגרת זהובה)** לארכיון + הזמנה לשיחה חדשה. הקטגוריה `דף מיזם` נשמרה. נוצר עם `create_only` לארכיון (אין דריסה).

### פרויקט בתי הקפה — 29 ערכים חדשים + תשתית
המשתמש ביקש ערך לכל בתי הקפה במושבה, עם תמונה ואנקדוטות (החלטות: **כל ~30**, **שימוש הוגן + לינק וידאו**).
- **רשימה מקיפה** נאספה ממקורות מסחריים/קהילתיים (rest, parkur, mako, d.co.il) — 29 בתי קפה/מאפיות.
- **קטגוריה חדשה** [[:קטגוריה:בתי קפה]] (תת-קטגוריה של "עסקים ותעשייה"). שים לב: הדף הקיים `[[בתי קפה]]` הוא על בתי קפה **קהילתיים** ("קהילה לדורות") — שונה.
- **דף מפתח** [[בתי הקפה של פרדס חנה-כרכור]] — מקובץ לעצמאיים / מאפיות-קלייה-פטיסרי / רשתות. מקושר מהעמוד הראשי באריח העסקים.
- **29 ערכים** נוצרו (`create_only`). דגלים עשירים עם אנקדוטות: **גבריאלה ורות** (סיפור הקליפ "בוקר פרדס חנה" של אברהם טל 2016 + לינק יוטיוב; נוסח כ"מספרים במושבה" כי לא אומת ב-100%), **נוני ופורטונה** (מקום אחד! חנות וינטג'+קפה, רות ומורן בן-יהודה, מאפים הונגריים), **לפה** (דניאל קיציס, סלט ביצים עד 11:00), **בלומס** (אסף חניה, קלייה), **קפה הצבי**. ~15 קצרמרים (שם+כתובת) על מקומות בלי מידע ברשת.

### ערך חדש: אורוות האמנים
לא היה ערך (בדקתי כל האיותים). נוצר [[אורוות האמנים]] — היסטוריה (אורוות בית הספר החקלאי שנות ה-30 → נטישה → שיקום 2017 ע"י קבוצת אמנים), המתחם היום, משמעות (התחדשות עירונית + הערת ג'נטריפיקציה ממחקר TAU), קישורים, קטגוריות. קישור הדדי עם [[קפה הצבי]].

### תיקון קישורים חשוב
**אין ערך-אב `[[פרדס חנה-כרכור]]` בוויקי** — כל קישורי שם-המושבה היו אדומים. תוקן ב-31 דפים: שם המושבה קושר ל[[היסטוריה של פרדס חנה-כרכור]], וקישורי `כרכור`/`רוטשילד` האדומים נוקו לטקסט.

### תמונות (שימוש הוגן)
המשתמש סיפק 3 תמונות ב-`~/tmp/cof/` (שם קובץ = שם ערך). הומרו ל-JPG ≤640px והועלו עם רציונל שימוש-הוגן + שובצו: **גבריאלה ורות** (חזית בוגונביליה), **נוני ופורטונה** (פטיו תחת הגפן), **אורוות האמנים** (שער הכניסה הצבעוני). Workflow מוכן להמשך.

## צעדים הבאים
1. **ערך-אב חסר: "פרדס חנה-כרכור"** — הוויקי נשען עליו ואין אותו. שווה ליצור טיוטה (הוצע למשתמש).
2. **תמונות ל-~27 בתי הקפה הנותרים** — אותו workflow: תמונה ל-`~/tmp/cof/` ששמה = שם הערך → העלאה+שיבוץ בשורה.
3. העשרת הקצרמרים שהמשתמש מכיר אישית (אווירה/היסטוריה/אנשים).
4. (מהסשן הקודם) תיר נמוך: גדמי בתי-ספר, מדריכי בנקים/קופ"ח, ~55 דפי רחובות.

## הערות
- כלל מחייב נשמר: get לפני edit, שליחת דף מלא, `create_only` ליצירות.
- כל הסקריפטים החד-פעמיים ב-scratchpad של הסשן (לא בריפו).
