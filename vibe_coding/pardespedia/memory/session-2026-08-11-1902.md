# סשן 11.8.2026

סשן ארוך. שלושה צירים: תשתית מדידה וגילוי, ניקוי חוב ערכים, וערכים חדשים.

## Google Analytics 4 — הותקן ופועל

הושלם מקצה לקצה. מזהה המדידה `G-9PGTWDGBKR`, נכס בשם "פרדספדיה".

- הקוד הודבק ב`מדיה ויקי:Common.js` (דורש הרשאת מפעיל מערכת, הבוט לא יכול).
- אומת ש-`load.php` מגיש את מודול `site` לגולשים אנונימיים ושהמזהה בקוד.
- אומת ב-Realtime שהנתונים מגיעים, עם שמות ערכים בעברית קריאה.
- **שמירת נתונים 14 חודשים** בשני השדות. זו ההגדרה שאינה רטרואקטיבית וחייבת להיקבע ביום הראשון.
- **עורכים מחוברים לא נמדדים** בכוונה. המשמעות: בדיקה מהדפדפן הרגיל של עורך תמיד תיראה כאילו זה לא עובד. יש לבדוק בגלישה בסתר.
- `allow_google_signals: false`, בלי רימרקטינג.
- סעיף גילוי "מדידת תנועה ופרטיות" נוסף לדף `פרדספדיה`.
- הקובץ `site_css/common-js-analytics.js` עודכן עם המזהה האמיתי.

## robots.txt ומפת אתר — חסום

`crawl-delay: 60` עדיין ב-`robots.txt`, ואין `sitemap.xml`. הוכן נוסח מתוקן ל-`robots.txt`.

**החסם:** האתר מתארח ב-**GreenGeeks** (שרת `ams203.greengeeks.net`), ואין פרטי גישה ל-cPanel בשום מקום בריפו או בזיכרון. הדרך פנימה היא דרך לוח הבקרה ב-`my.greengeeks.com`, שיש בו כניסה ל-cPanel בלחיצה אחת. **שאלה פתוחה:** האם חשבון האירוח בכלל שייך לאורי, או למי שהקים את האתר ב-2019.

עוקף חלקי אם אין גישה: אפשר לייצר `sitemap.xml` ולהגיש ל-Google Search Console בלי לגעת בשרת. את `robots.txt` אי אפשר לתקן בלי גישה.

## תיקון `update_index.py` — באג מבני

הסקריפט היה **append-only**: חישב `missing = כל הדפים - מה שכבר באינדקס` ומעולם לא הסיר שורות. התוצאה: 68 שורות מתות מתוך 707 (66 הפניות, 2 דפים מחוקים).

**התיקון:** נוספו `norm()`, פרמטר `drop` ל-`build()`, וחישוב `stale` ב-`main()`. לפני ההרצה בוצעה בדיקת בטיחות שכל שורה שמוסרת מובילה ליעד שכבר באינדקס. הורצה: נוספו 5, הוסרו 68. הרצה חוזרת מתייצבת.

**חוב שנותר:** תיאורי האינדקס הם תצלום רגע ואינם מתרעננים. מדידה: **218 מתוך 644 תיאורים** לא תואמים את הערכים, ו-114 תמונות. **אין להריץ רענון גורף** — נבדק שהוא היה משפר 6 שורות ומשחית 11 (`בלומס` היה הופך ל-`]`, `הסברים ועוד` ל-`／+`). הצעד הנכון: קודם לתקן את `make_description` שיזהה פלט קצר או בלי עברית וישאיר את הקיים, ורק אז להוסיף `--refresh` עם דוח לפני כתיבה.

## ניקוי חוב ערכים

- **31 גני משחקים** נורמלו: קישורי רחוב ושכונה, `ראו גם` לרשימת גני המשחקים (קודם אף גן לא הצביע חזרה), תבנית `{{הרחבה}}` אחידה. מ-49-493 תווים ל-192-625, בלי קישורים אדומים.
- **3 עסקים** הורחבו במחקר רשת: `סוזאנה` (התברר כעגלת קפה ולא בית קפה), `טאבון הלויים` (סניף נוסף בבית שאן; האתר הרשמי מת), `Viola bar`.
- **7 רחובות** הורחבו ונוסחו מחדש בלשון אנציקלופדית.
- באגים שתוקנו: `{{{הרחבה}}` ב`השמינית (רחוב)`, `בב[[פרדס חנה-כרכור]]` ב`גולן (רחוב)`.

**חוב שנותר:** 96 ערכים בלי תמונה. רשימת צילום קונקרטית: גן הבלונים (גבע 5), גן תפוזים (האשכולית 25), גן אנקור (האנקור/נץ), ושכונות חלומות כרכור, יפה נוף ופסטורליה.

**שני ערכים חסרים שהתגלו:** `נווה התות (שכונה)` ו`קורן (שכונה)`, שתיהן קישורים אדומים ושכונות אמיתיות.

## ערכים חדשים

- **`קהילילה לבן`** — הלילה הלבן הקהילתי, מהדורת 27.8.2026. הנתונים נמשכו ממקור ה-JSON שמאחורי אתר האירוע (57 מוקדים, אזורים, שעות, קהלי יעד). הוצלבו 21 עסקים משתתפים שיש להם ערך בוויקי. נוסף ללוח האירועים עם `pin: true` ב-`manual_events.json`, כי החלון הוא 14 יום והאירוע היה רחוק 17.
- **`כינויים לשכונות במושבה`** — ערך הומוריסטי, טבלה עם תמונה/שכונה/כינוי/מקור. מקושר מארבעה ערכי שכונות. ההסברים בוססו על נתוני הוויקי (למשל "רובע הברון" כשם שיווקי של עומרים).
- **`מפת המחזור של פרדס חנה-כרכור`** — נותחה מפת My Maps של מנהל קיימות במועצה: 899 נקודות בשבע שכבות, 21 מרכזי מחזור בשמותיהם. הצילום מקושר כך שלחיצה עליו פותחת את המפה החיה.

## תיקונים לערכים קיימים

- **`חלומות כרכור (שכונה)`** — הוסרו שתי טענות לא מבוססות. תחקיר מלא: הכינוי "חלומות בלהה כרכור" הוכנס ב-2019 בדף כפול, והבוט מיזג אותו לערך הראשי ב-14.6.2026 בלי להבחין בין עובדה לבדיחה. הכינוי הועבר לערך ההומוריסטי, שם מקומו.
- **`אייל כגן`** ו**`תוכנית המתאר הכוללנית`** — נוסף תיאור מדיניות התכנון לפי פרסום מאוגוסט 2026, במפורש כמקור ראשוני ומיוחס, כולל הביקורת הציבורית שעלתה. סעיף "המצב כיום" בתוכנית המתאר היה ריק לחלוטין עד עכשיו.
- **`רומי נטיס`** — תיקוני פרטים ותמונות לפי פנייה של נושאת הערך.
- **ערכים שנגעו בהם השבוע** — נוספו תמונות היסטוריות בנחלת הכלל ל`בית קפה טיצ'ר` ול`עמדת שמירה היסטורית ברחוב המייסדים`, ותמונה חופשית מפיקיוויקי ל`המייסדים (רחוב)`.

## לקח מהסשן

נכתב ערך שלם בזמן עבר על אירוע עתידי, ותושבת הצביעה על כך. **כלל להמשך:** לפני כתיבת ערך על אירוע מפיד או מתוכנייה, להשוות את תאריך האירוע להיום ולכתוב בעתיד כל עוד לא עבר. את `קהילילה לבן` יש להעביר לזמן עבר אחרי 27.8.

## הנחיה חדשה שנשמרה לזיכרון

כל קישור לגוגל מפות בפרדספדיה חייב `hl=iw` כדי שייפתח בעברית. נסרקו 105 קישורים בוויקי, 101 היו תקינים, 4 תוקנו. אומת בפועל שזה עובד.

## צעדים הבאים

1. **cPanel** — לברר בעלות על חשבון GreenGeeks, לתקן `robots.txt`, לייצר ולהגיש `sitemap.xml` ל-Search Console. זה הדבר בעל ההשפעה הגדולה ביותר על כניסות.
2. **`make_description`** — לתקן, ואז להוסיף `--refresh` לאינדקס עם דוח לפני כתיבה.
3. **סבב צילום** לגנים ולשכונות ברשימה למעלה.
4. **דוחות GA** — לבדוק אחרי 24 שעות מה עובד.
5. `קהילילה לבן` לזמן עבר אחרי 27.8.
6. שני ערכי השכונות החסרים.
7. מחיקת קובץ שהועלה בטעות: `קובץ:עמדת שמירה ברחוב המייסדים - 2022 מבט לחלל הפנימי.jpg` (דורש מפעיל מערכת).
