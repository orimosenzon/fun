# סשן 2026-07-12 (פרדספדיה) — קטגוריית גמלאים, ערך גיא לב

## מה נעשה

### פתיחת סשן
- git pull (עדכונים מסשני 9–11.7 נמשכו).
- ה-hook עדכן את לוח אירועי התרבות (rev 4220, 7 אירועים). אין הודעות ממתינות בדף השיחה של הבוט.

### מועדוני גמלאים
המשתמש חיפש ערכים על מועדוני גמלאים. נמצא שקיימת [[קטגוריה:אזרחים ותיקים]] (מועדון למרחב, מועדון פטאנק, מרכז הפיס לגיל הזהב, מרכז קהילתי אור + תת-קטגוריה קהילה לדורות), אך המילה "גמלאים" לא הופיעה בשום ערך. לפי בחירת המשתמש (הפניה + השלמה, במקום קטגוריה כפולה):
- [[מועדון קיוקו]] נוסף לקטגוריית אזרחים ותיקים — היה היחיד שחסר בה (rev 4221).
- נוצרה [[קטגוריה:גמלאים]] כהפניה ל[[קטגוריה:אזרחים ותיקים]] (rev 4222).

### ערך חדש: [[גיא לב]] (rev 4224)
יזם קהילתי, פעיל חברתי ומעצב מוצרים דיגיטליים ("דיגיטל גורו"). מיוזמי [[פסטיבל דרך הנדיב]] וממחדשי ה[[שיקשוק]]. מקורות: ראיון בגפן מגזין המושבות 11/2022 (אימות מגורים — כ-20 שנה במושבה), שיחה מצולמת ביוטיוב, דיווח N12 על תקיפתו בהפגנה (10/2020, הוזכר בניטרליות). כתבת מגדלור ניוז חסומה (Cloudflare) — פרטים שהופיעו רק בה (קהילות נוספות, מימון המונים) לא נכללו.
- תמונה: `גיא לב.jpg` — שימוש הוגן, 480×640, מתוך כתבת גפן (הוא ליד כרזת הפסטיבל).
- קישוריות: שורה 26 ב[[דמויות בפרדס חנה-כרכור]] עם מספור מחדש (rev 4225), אזכורים מקושרים ב[[פסטיבל דרך הנדיב]] (rev 4226) וב[[שיקשוק]] (rev 4227), שורה ב[[האינדקס של הדפים]] (rev 4228, ידנית — ראו למטה).

## בעיה פתוחה — דף תועה "שיחת עמוד ראשי" במרחב הערכים
ריצת ה-cron של 12.7 בבוקר יצרה הערת שקיפות (תיקון "ערוות האמנים"→"אורוות האמנים" ב-rev 4216) בדף בשם "שיחת עמוד ראשי" **במרחב הערכים**, במקום בדף השיחה האמיתי "שיחה:עמוד ראשי". בגלל זה `update_index.py --write` לא הורץ (היה מוסיף את הדף התועה לאינדקס) — שורת גיא לב נוספה לאינדקס ידנית. ניסיון להעביר את התוכן ולמחוק נחסם ע"י מסנן ההרשאות; ממתין להחלטת אורי:
1. להעביר את ההערה ל"שיחה:עמוד ראשי" ולמחוק את הדף התועה (ייתכן שנדרש חשבון sysop).
2. לתקן את הפרומפט/סקריפט של ה-cron שלא ייצור שוב דפים כאלה.

## תיעוד
- הסקיל `~/.claude/skills/pardespedia-person/SKILL.md` שמתועד ב-project-knowledge.md **כבר לא קיים** — העבודה בוצעה לפי כללי הזהב שבתיעוד. project-knowledge.md עודכן בהתאם.

## צעדים הבאים
1. הדף התועה "שיחת עמוד ראשי" — טעון הכרעה (ראו למעלה).
2. לעקוב אחרי ההרצה הראשונה של `cron_contributor_comments.sh` (מחזור 3 ימים) — לבדוק `contributor_cron.log`.
3. תור ההעשרה הישן (49 ערכים) — עדיין ממתין.
4. [[ויקטור קוהן]] — OCR שבור, פתוח.
5. עמנואל ליבוביץ (מגורים) — לא הוכרע; שאלת עדן אוליאל בעמוד הראשי — פתוחה.

## הערות
- כל עבודת התוכן בוצעה מול ה-API החי (get-first בכל עריכה על דף קיים), ללא שינויי קבצים מקומיים.
- קבצים לא-עוקבים שלא נגעתי בהם: `pipin/pipin_world.db` (פרויקט אחר).
