# Session 2026-03-21

## מה עשינו

### ארגון מחדש של תיקיות השיעורים
- שינוי שמות מספריים לשמות נושאים:
  - `lesson01` → `function_fit_and_gd/function_fit_and_gd.ipynb`
  - `lesson03` → `backpropagation/backpropagation.ipynb`
- יצירת תיקיית `intro_to_ai_ml_dl/` חדשה

### פרויקט material
- ייבוא חומר הגלם מהקורס הישן (Google Drive → תיקיית `material/`)
- זיהוי תמונות רלוונטיות: `ai_timeline.png`, `ai_venn.png`, `types_of_ml.png`, `agent_environment.png`

### שיעור intro_to_ai_ml_dl — נוטבוק חדש ומלא
תוכן השיעור (בהשראת השיעור הישן, עם שיפורים):
1. היסטוריה + ציר זמן (`ai_timeline.png`)
2. הגדרות AI / ML / DL
3. ויזואליזציית עיגולים מקוננים (matplotlib) — אהובה על המשתמש
4. תכנות רגיל vs למידת מכונה (אנלוגיית מתכון לעוגה)
5. קטגוריות ML + `types_of_ml.png`
6. Supervised Learning — MNIST עם **סליידר אינטראקטיבי** (ipywidgets) + גרף התפלגות ספרות
7. Unsupervised Learning — הסבר
8. Reinforcement Learning — `agent_environment.png` + סרטון Breakout מוטמע (IFrame YouTube)

### base64 embedding
- כל התמונות בנוטבוק הוחלפו ל-base64 embedded — הנוטבוק עצמאי לחלוטין, מתאים ל-Google Drive/Colab

### תיקון RTL ב-VS Code Claude extension
- הוסיף `unicode-bidi: plaintext` ל-`webview/index.css` של התוסף
- מקביל ל-`dir="auto"` שנעשה בפרויקט golem
- תועד בזיכרון: `feedback_vscode_rtl_fix.md`

## Next Steps

1. בדיקת הנוטבוק `intro_to_ai_ml_dl` ב-Jupyter / Colab — לוודא שהסליידר עובד, הסרטון מוצג
2. הוספת תוכן נוסף ל-`intro_to_ai_ml_dl` (למשל: categorical vs regression, דוגמאות מהחיים)
3. `function_fit_and_gd` — לבדוק שהוויזואליזציה הכפולה (קונטור + פרבולה) עובדת אחרי שינוי שם הקובץ
4. `backpropagation` — להוסיף ווידג'ט אינטראקטיבי (כמו ב-function_fit_and_gd)
5. חשיבה על סדר השיעורים ההגיוני כשהקורס יהיה שלם
