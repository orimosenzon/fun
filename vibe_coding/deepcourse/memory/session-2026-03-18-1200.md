# Session 2026-03-18-1200

## מה עשינו
יצירת `lesson03_backprop.ipynb` מאפס — שיעור על Backpropagation.

### מבנה השיעור
1. **מוטיבציה** — למה לא אפשר לגזור ביד עבור רשתות גדולות
2. **רקע היסטורי** — ורבוס (1974), לינאינמאה (1970), רומלהארט/הינטון/וויליאמס (1986), AlexNet (2012), נובל לפיזיקה (2024)
3. **חוק השרשרת** — הבסיס המתמטי, דוגמה קונקרטית עם אימות נומרי
4. **Mini-Network** — 3 פרמטרים: w, b, v עם ReLU ו-MSE Loss
5. **גרף חישובי** — ויזואליזציה matplotlib עם חיצים קדימה ואחורה
6. **Forward Pass** — קוד עם verbose, cache, הסבר ReLU פעיל/חסום
7. **Backward Pass** — קוד שלב אחר שלב
8. **Gradient Check** — אימות נומרי (הפרשים סופיים)
9. **הדגמה אינטראקטיבית** — ipywidgets: כפתורי ◀/▶, 11 שלבים (0-10), גרף חי

### פיתרון בעיות
- **עברית הפוכה ב-matplotlib**: `python-bidi` + `get_display()` על כל מחרוזת עברית
- **אזהרות Glyph missing**: `warnings.filterwarnings('ignore', message='Glyph.*missing from font')`
- **ניווט שלבים**: `state = {'step': 0}` (dict מוטציה), לא `int` פשוט

## מצב נוכחי
lesson03 (backpropagation) — מלא ועובד. 17 תאים.

## Next Steps
- lesson04 — נושא אפשרי: רשת עם מטריצות משקולות (matrix backprop)
- `project-knowledge.md` עם מבנה הקורס המלא
