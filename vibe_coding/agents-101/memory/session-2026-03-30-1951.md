# Session 2026-03-30-1951

## מה נעשה בסשן זה

### הוספות למחברת `agents_101.ipynb`
נוספו 3 תאים חדשים אחרי ה-intro של LangGraph (לפני הקוד):
1. **"למה דווקא גרף"** — nodes/edges/state, הסבר על מחזוריות vs DAG, אנלוגיה ל-Redux reducer
2. **CoT ו-ToT** — Chain of Thought כנתיב לינארי, Tree of Thoughts כעץ עם expand/evaluate/prune. טבלת השוואה: Vanilla → CoT → ReAct → ToT → MCTS
3. **קוד ToT** — מימוש מינימלי ב-LangGraph: `expand → evaluate → answer`

### קובץ חדש: `agent_demo.py`
סוכן LangGraph להרצה עצמאית מהטרמינל:
- כלי 1: `calculate` — חישוב אריתמטי בטוח דרך AST (לא eval גולמי)
- כלי 2: `web_search` — DuckDuckGo דרך ספריית `ddgs`
- גרף: `START → agent ⇄ tools → END`
- זיכרון קבוע בין הרצות: `SqliteSaver` → `agent_memory.db`
- מודל: `claude-haiku-4-5-20251001`
- המפתח מוטמע ישירות בקוד (הקובץ ב-.gitignore)

### `.gitignore` חדש
נוצר `/agents-101/.gitignore` עם:
- `agent_demo.py` (מכיל API key)
- `agent_memory.db` (היסטוריית שיחות)

### תלויות חדשות ב-`requirements.txt`
- `ddgs` (שינוי שם מ-`duckduckgo-search`)
- `langgraph-checkpoint-sqlite`

## Next Steps
1. להריץ `agent_demo.py` end-to-end ולוודא שעובד (חישוב + חיפוש)
2. להריץ את `agents_101.ipynb` מתחילה לסוף ולוודא שכל הcells עוברות
3. שקול להוסיף `__pycache__/` ל-.gitignore
