# Session 2026-04-14 17:03

## מה נעשה
- נוצר `api-guard.js` — מודול Kill Switch כללי לניהול לימיטים על קריאות API
  - מעקב יומי ודקתי ב-localStorage
  - Hard stop עם banner אדום בראש המסך כשעוברים את הלימיט
  - API: `check()`, `configure()`, `getStats()`, `reset()`, `showBanner()`
- שולב ב-`index.html`:
  - טעינת `api-guard.js` לפני הסקריפט הראשי
  - `ApiGuard.check('gemini')` לפני כל קריאה ל-Gemini
  - `ApiGuard.check('places')` לפני כל קריאה ל-Places API
  - לימיטים: Places 50/day, 8/min | Gemini 150/day, 20/min | Maps 30/day

## רקע
המפתחות הישנים נגנבו וגרמו לחיוב של 5500 ₪. המשתמש ניקה את ה-API Keys בגוגל קלאוד.

## Next Steps
1. ליצור מפתחות API חדשים ב-Google Cloud עם הגבלות:
   - HTTP referrer restriction לדומיין בלבד
   - API restriction: מפתח נפרד ל-Maps+Places, מפתח נפרד ל-Gemini
2. להחליף את המפתחות בקוד (שורות 1280-1281 ב-index.html)
3. שקול להעלות את `api-guard.js` כ-Open Source (GitHub Gist / repo נפרד)
